# A股投资平台 · 工程规则

唯一设计入口：[docs/rebuild/README.md](docs/rebuild/README.md)，revision `a-share-platform-rebuild.v1.0`。
用户已授权全量重建；历史实现保存在仓库外恢复目录与 Git 历史中，不得重新接入旧业务服务。

- 量化模型与 Agent 共同参与选股及持仓管理。缺少有效联合版本时返回评估不可用；禁止公式兜底伪装生产预测。
- Python kernel 是在线与回测唯一计算内核。Web 只消费生成合同，不计算交易动作或权威余额。
- PostgreSQL 是业务事实源；成交、现金、批次和冲正必须事务化、幂等、可对账。计划确认不等于成交。
- 金额/价格使用 Decimal 与十进制字符串；数量使用整数股；时间存 UTC，交易日使用上海时区。
- 市场事实、Agent 产物、预测、模拟结果、真实成交分开保存。没有可信历史证据不得声称联合模型收益已验证。
- 每个模块只有一个写入责任方。迁移不作为在线依赖；任何真实账户切换必须先对账且只有一个写入方。
- 密钥仅在仓库外配置或部署注入；不得输出到日志、浏览器、提交。测试使用合成账户。
- 按纵向切片实现、验证、提交；任务真实进度记录在 `docs/rebuild/IMPLEMENTATION.md`。

实际开发与检查命令以 README 和锁文件为准。尚未完成的能力不得在文档中标记验收通过。
