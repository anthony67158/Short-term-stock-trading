# 机会雷达任务清单

> 状态：实施中
> 规格：`docs/机会雷达融合规格.md`
> 技术计划：`docs/机会雷达技术计划.md`

## 任务 1：建立统一领域合同

- [x] 新增 `shared/opportunityRadar.js`
- [x] 定义交易阶段、三条 lane、候选状态和来源状态
- [x] 实现股票去重与来源合并
- [x] 实现入场、止损、止盈、时间退出投影
- [x] 实现过期与局部缺失降级
- [x] 新增 `test/opportunity-radar.test.js`

验收：

- 相同代码只输出一次。
- `READY` 必须同时具备方向资格和完整价格合同。
- 每个可执行候选都有 `entryPlan` 与 `exitPlan`。
- 输入顺序变化不影响最终排序。

## 任务 2：增加只读聚合 API

- [x] 新增 `api/opportunity_radar.js`
- [x] 并行读取板块、公式、尾盘权威快照
- [x] 使用 `Promise.allSettled` 支持局部降级
- [x] 加入账号鉴权、超时和 `no-store`
- [x] 新增 `test/opportunity-radar-api.test.js`

验收：

- 正常请求返回 `opportunity-radar.v1`。
- 单个来源失败仍返回其它来源及明确状态。
- 聚合读取不调用 LLM、不抓取行情、不写 OSS。

## 任务 3：实现统一机会雷达 UI

- [x] 新增 `src/opportunityRadarClient.js`
- [x] 新增 `src/components/OpportunityRadar.jsx`
- [x] 新增摘要、方向带、候选行和来源状态组件
- [x] 实现“提前布局 / 盘中机会 / 次日计划”切换
- [x] 实现候选展开和加入自选
- [x] 实现加载、空态、过期、局部失败状态
- [x] 新增 `test/opportunity-radar-ui.test.js`

验收：

- 单一主面板内先结论、后机会、再证据。
- 每个候选显示入场和退出计划。
- 接近公式与纯板块候选不会显示为可买。
- 移动端保持单列且操作控件不溢出。

## 任务 4：替换今日决策入口

- [x] `TodayTab.jsx` 改为只挂载 `OpportunityRadar`
- [x] 移除页面上的独立 `SectorForecast` 与 `FormulaSelection`
- [x] 更新原入口顺序测试
- [x] 保留旧组件和接口供回滚及个股详情复用

验收：

- “今日决策”只出现一个机会决策模块。
- 候选池与涨停池位置保持不变。
- 旧数据和定时任务无迁移要求。

## 任务 5：统一刷新与进度

- [x] 按视图调用对应现有生成任务
- [x] 并行展示各来源真实阶段
- [x] 防止重复提交和跨视图重复入队
- [x] 完成后重新读取统一快照
- [x] 支持刷新后恢复任务状态

验收：

- 一个主按钮完成当前视图需要的更新。
- 任一路失败不撤销其它来源的成功结果。
- 不出现虚假进度或无限加载。

## 任务 6：样式与响应式

- [x] 新增机会雷达布局样式
- [x] 复用现有 token、按钮和图标
- [x] 明暗主题保持中性冷灰蓝表面
- [x] 验收 320、768、1024、1440 四个宽度
- [x] 检查键盘焦点、ARIA 和触屏目标

验收：

- 无卡片套卡片。
- 无大面积非语义红色。
- 无横向滚动、文字遮挡和按钮压缩。

## 任务 7：文档、回归与部署

- [x] 更新 `docs/DESIGN.md`
- [x] 运行 `npm test`
- [ ] 运行 `npm run harness:sector`
- [x] 运行 `npm run build`
- [ ] 提交并推送
- [ ] 部署 Vercel
- [ ] 打包并部署阿里云 FC
- [ ] 验收两个前端域名和 FC API

完成标准：

- 全部自动化测试通过。
- Vercel 与 FC 使用同一构建资源。
- 两个前端入口和核心 API 均返回 `200`。
