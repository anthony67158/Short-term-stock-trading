# 重建实施记录

## 授权与目标

用户已在2026-09-15明确要求开始全量重建与替换，必要时重新训练A股量化模型，系统完成后通过browser-use跑全部流程并修复。按`a-share-platform-rebuild.v1.0`执行；选股与持仓管理必须由量化模型和Agent共同参与。

Agent服务指定为`https://linlongs.com`，实际API模型ID为`gpt-5.6-terra`。180秒预算下真实只读生产路径已生成并通过本地合同校验；供应商仍存在历史尾延迟，研判质量和重复运行稳定性尚未验收，禁止擅自替换模型或伪造结果。凭据保存在仓库外私密配置，任何提交、日志和浏览器不得包含明文。

## M0恢复点

- 初始提交：`7c153939f120ca16bda83d1fd2de6a548d300767`。
- 原工作区带大量未提交增删改，已保存tracked/untracked快照、删除清单、binary patch和状态。
- 私密恢复目录：`/Users/bytedance/Desktop/stock-dashboard-recovery-20260915`，目录权限700。
- 压缩快照包含1,313个文件，已逐文件读回并核验SHA-256。
- 旧环境配置与凭据独立保存在恢复目录private下，文件权限600；未写入Git。
- 重建分支：`rebuild/platform-v1`，在共享工作区创建；旧业务树已隔离归档，清理提交`d6d5d5c6`。
- 私密运行配置：`/Users/bytedance/.config/stock-platform/platform.env`，权限600；Agent与豆包搜索凭据均未进入仓库。

## 环境探测

本机已有Node22、pnpm10、uv。已安装PostgreSQL17.11，并在仓库外建立独立本地集群，
仅监听127.0.0.1:55432，TCP采用SCRAM认证。数据库与秘密配置在
`~/.config/stock-platform/`；新应用只加载`platform.env`，不自动加载旧`.env`。
Python3.12及依赖由`backend/uv.lock`锁定。云端托管PG、容器部署与成本验证尚未完成。

## 当前进度

| 任务 | 状态 | 证据/下一步 |
|---|---|---|
| T00规格与追溯 | 已完成设计核验，已获实施授权 | 文档结构、链接、55项任务依赖与20组AC检查通过 |
| T01恢复点 | 已完成备份及清理清单 | recovery目录`cleanup-plan.json`与`legacy-tree/`保存106项移出资产；包括ignored数据、模型、资料，无资产销毁 |
| T02新工程 | 后端与Web独立启动、构建通过 | 根AGENTS/README重写；新工程不引用旧服务 |
| T03基础合同 | 已实现首批并生成客户端 | Decimal字符串、严格股数、UTC时间、错误包；`pnpm contracts`通过，随业务扩展 |
| T04 UI基础 | 深浅token/控件/登录/导航已实现 | browser-use验证登录页472px无横向溢出；全视口及完整工作区尚待后续验收 |
| T05运行验证 | 本机数据库与任务验证通过，云端未验证 | Alembic迁移、并发领取、幂等、租约隔离与取消测试通过 |
| T06身份 | 会话及基础安全已实现 | Argon2、HttpOnly cookie、注销/过期、Origin校验、共享限流；账户级授权随后扩展 |
| Agent连通性 | 真实只读链路可完成，质量与稳定性待验收 | `gpt-5.6-terra`在180秒预算内完成搜索规划、3次豆包检索和结构化研判；历史尾延迟仍保留为风险 |
| 应用/模型/全流程验收 | 持续实施中 | 联合架构已作为`SHADOW`发布并禁止真实账户新增风险；前瞻Agent样本、联合训练、真实账本迁移、云部署和切流未完成 |

### 首批验证（2026-09-15）

- `uv run alembic upgrade head`：0001任务/outbox、0002身份迁移成功。
- `uv run platform-cli health`：实际PostgreSQL连接成功。
- `pytest -q backend/tests`：6项通过，覆盖金额/股数、并发幂等、租约fencing、
  取消发布、会话/注销/过期、Origin与登录限流；存在2项第三方弃用警告，后续锁定兼容版本处理。
- `pnpm contracts`：实际生成OpenAPI与TS；`pnpm build`包含TypeScript检查并构建成功。
- `pnpm lint`：后端Ruff通过（前端lint后续补齐）。
- browser-use发现并修复CSS token导入层级错误；实际页面标题、登录form和深色背景正确，
  472px视口无横向溢出。发现浏览器恢复到旧生产域名后立即关闭，未执行任何生产写入。
- 框架依据：FastAPI multiple files、SQLAlchemy2 Session Basics、openapi-ts/openapi-fetch官方文档。

## 后续交付记录

### 账户资金切片（T13/T16/T18 的首批范围）

- 实现账户创建、实盘/模拟隔离、显式单股风险上限、期初/入金/出金、分页流水。
  PostgreSQL迁移0003包含账户归属、唯一幂等键、版本与资金符号约束。
- 资金写入锁定账户，校验版本与归属；重复请求返回原记录，内容变化返回409；
  不允许未来资金事实、超额出金、重复期初；按发生顺序录入。
  金额采用Decimal；账本更新和`portfolio.changed` outbox同事务提交。
- Web组合与执行页面接入生成的API类型，支持创建、选择账户、资金录入、
  错误反馈、流水分页和刷新恢复。纯现金余额不冒充净资产或可用资金。
- `uv run pytest -q`：8项通过（含并发重复写、精确金额、跨账户访问、错误金额、
  版本与余额约束）；`alembic check`确认数据库与声明无漂移；构建和Ruff通过。
- `pnpm test:e2e`：真实Chromium完成创建→期初10,000.01→刷新→拒绝超额出金
  →出金200.02→余额9,799.99→登出→刷新，1条完整场景通过。
  同场景检查390/768/1280/1440深浅主题无页面横向溢出，保留8张本地截图。
- 浏览器发现并修复：长用户名挤出移动端按钮；清空整个QueryCache导致登出后
  活跃observer仍展示私人页面。新逻辑先更新会话再移除其他查询。
- browser-use MCP已验证本地登录页面；该工具禁止代填密码。账户内流程通过
  Playwright调用真实登录接口建立临时合成会话后执行，测试结束清理合成账户。
  这不代表全部平台流程已完成browser-use验收。
- 限制：证券、持仓、成交、预留、冲正、公司行为尚未进入本切片，M2未完成；
  本地验证不代表生产部署。两条第三方依赖弃用警告仍待处理。

### 当前证券目录与报价切片（T07/T08/T12 部分）

- 独立实现新浪当前A股目录与腾讯报价Adapter，没有调用旧业务服务。
  东方财富目录探测连接失败，未作为必要依赖。行情格式为实际端点采样验证，
  尚无商业授权/SLA，不声称具备机构历史行情完整性。
- `uv run platform-cli sync-instruments`真实下载5,561只证券；56页按symbol排序，
  逐页数量、全局唯一、头尾总数检查通过才原子发布。保存成员快照及SHA-256，
  未收录证券仅标记不在当前目录，不擅自判定退市。首次观察时间不冒充上市时间。
- 证券代码保留前导零与交易市场；目录明确`CURRENT_ONLY`。研究报价保存行情时间、
  接收时间、来源、缺失与过期状态；所有报价暂不具备执行资格。
- Web市场支持搜索、分页、关注列表；股票详情展示报价和来源时间、
  加入/移出关注。尚无Agent论点或联合预测时明确展示缺口。
- `uv run pytest -q`10项通过；`alembic check`无漂移；OpenAPI客户端生成、
  TypeScript与Vite构建、Ruff通过。
- 浏览器场景追加真实搜索000001→平安银行→读取报价→加入关注→返回列表
  →刷新恢复→移出关注，完整场景通过（4.5秒）。临时测试身份和私有数据自动清理。
- 限制：历史证券身份、交易日历、日线、财务与公告尚未实现；
  公开源当前快照不能充当历史因果股票池，T07/T08/T12未全部完成。

### 规范历史行情数据集（T07/T08/T21 前置）

- 新增仓库外、可恢复、封存后不可写的SQLite数据集构建器和CLI；按证券参考、
  交易日分区和最终封存执行。重复源行幂等，内容冲突、重复键、6000行截断风险、
  复权因子缺失及无停牌解释的行情缺口均硬失败。
- 证券目录覆盖上市、暂停和退市状态，按请求日期区间构造因果股票池。北交所
  旧代码通过映射表归入稳定920身份，保留源代码与原上市日；A股池最早从
  2021-11-15北交所开市日起纳入。
- 真实2026-09-15探测封存5,562只证券、5,548条日线、5,562条复权因子和
  17条停复牌事实，覆盖主板、创业板、科创板、北交所。SQLite完整性检查通过，
  manifest数据库SHA-256独立重算一致。
- 后端65项测试及Ruff通过，新增覆盖批量冲突回滚、恢复重放、封存不可写、
  因果上市/退市范围、停牌解释、北交所映射、上游截断和仓库外路径限制。
- 当前仅完成单日真实验收；2016年至今全量下载、曾用名、全市场分钟策略、
  独立公开源交叉核验、标签重构、训练及回测尚未完成，不能标记T07/T08通过。
- 增量加入3条交易所公告佐证的证券代码迁移，以及12段暂停上市期间。
  同主体新旧代码仅在逐值一致时按当日有效代码保留；北交所上市资格前的新三板
  回填、退市后延续的复权因子按证券生命周期分类丢弃，数量和源行SHA-256进入
  checkpoint。其他证券或流类型越界继续硬失败。
- 新建仓库外v4数据集，真实完成2016-01-04、2020-01-02、2021-11-15三个
  边界开放日，累计10,895条日线、11,189条复权因子和323条停牌记录；
  SQLite完整性为`ok`。三日分别验证北交所开市前、退市后因子延续、开市日
  未来北交所证券回填，长期无日线证券均由官方暂停上市期间闭合。
- 本增量后端全量72项测试和Ruff通过。全仓`ruff format --check`仍报告74个
  历史文件未格式化，本切片只格式化改动文件，未制造无关迁移和测试改写。
  全开放日下载尚未完成，因此T07/T08仍不标记通过。
- 全开放日下载推进中在2016-09-06发现`SZ.300372`缺口。深交所公告确认其
  当日起暂停上市、2017-07-17进入退市整理期；补充官方区间和回归测试后该日
  真实分区通过。当前连续checkpoint已推进至2017-04-10，仍未完成全区间。

### 按候选证券获取5分钟数据（T08/T21 前置）

- 新增显式`minute`构建阶段，只允许传入唯一、合法的证券ID。每个证券日必须
  先有已验证日线分区和当日日线，不提供全市场无界分钟下载入口。
- 写入前严格检查48个标准5分钟时点、稳定证券身份、开收盘、日内价格范围、
  成交量小于100股偏差，以及成交额2元或5bps门限；聚合值、实际偏差、门限和
  日线源行hash进入checkpoint。任一失败不写分钟行，不插值、不改写日线。
- 北交所分钟接口真实要求当前稳定代码，不能沿用日线历史旧代码；构建器保留
  两类源代码并映射到同一稳定身份，已有合成回归覆盖。
- 真实写入2021-11-15主板/创业板/科创板和2022-11-15北交所四个样本分区，
  共192根K线，SQLite完整性为`ok`。北交所开市首日虽返回48根，但成交额偏差
  约10.03bps，超过门限后被拒绝且零写入。
- 后端累计78项测试及Ruff通过。该切片只证明四类股票可按需获取和严格验收；
  候选episode分钟覆盖率尚未随回测生成，T08/T21仍不标记完成。

### 独立公开源日线交叉核验（T08 前置）

- 新增新浪与腾讯历史日K严格解析器及只读交叉审计CLI，报告逐样本保存源可用性、
  OHLC比较、成交量差、源行hash和总报告hash，不把公开端点作为写入事实源。
- 真实核验2022-11-15与2026-09-15四板块共8个样本全部通过。新浪8/8一致；
  腾讯沪深6/6一致，成交量差0～37股；腾讯北交所历史行缺失被明确记录。
- 探测发现腾讯科创板volume已是股，而沪深主板/创业板为手；按板块修正单位并
  增加协议测试，没有用放宽容差掩盖百倍单位错误。报告与hash见
  `market-data-readiness.md`。
- 该结果是跨年份分层抽样，不是全库逐行双源验证；两个公开源也未独立提供本次
  成交额核验。全量规范库完成前T08仍不标记通过。

### 证据与研究运行器切片（T09/T10/T11 部分）

- 手工原文入库、HTTPS来源与发布时间、逐字引用校验、内容哈希、用户隔离、
  不可变修订；`USER_SUPPLIED/QUOTE_MATCHED`明确区分用户材料与已核验外部事实。
  引用匹配不表示原文真实；目前不自动抓取用户URL。
- Research任务冻结材料包、问题、asOf、模型、协议与截止；预算按用户及全平台
  24小时调用次数限制，单包60,000字符、最多16份材料、单次输出2,500 token。
  服务默认未启用，避免反复请求已知鉴权失败的端点。
- 独立Worker以PG租约领取；调用前标记外部步骤，单次HTTP调用含墙钟截止与
  最大响应体限制。结构化输出拒绝越权字段、包外引用、无法逐字定位的OBSERVED、
  无效期限。事实/推断/假设分型保存，所有研判仍是RESEARCH_ONLY。
- 研判、任务终态和outbox在同一fenced事务提交；取消发生在调用期间时不保存研判。
  前端提供材料选择、研究问题、实际任务状态、停止与论点版本列表，任务ID保存在
  URL用于刷新恢复。Agent不可用时按钮禁用并显示具体原因。
- `uv run pytest -q`13项通过，含合成HTTP协议、非法引用、生成中取消、
  不可变原文及跨用户读取。模型成功测试使用合成响应，未调用真实供应商。
  TypeScript/Vite构建通过；浏览器追加材料保存→刷新→引用展开→选择材料
  →研究不可用状态，完整场景通过（5.6秒）。
- 限制：自动公告/财报原文采集、多角色研究、完整论点版本编辑、
  历史研判与联合特征尚未实现；搜索结果仍是待核验发现线索。
  测试通过不代表真实研判质量已验收，M1尚未完成。

### 申报数量与成交账本切片（T14/T15/T16/T18 部分）

- 新建纯Python交易内核：按沪深主板、科创、创业板、北交区分限价数量；
  生效/核验区间与官方来源见`trading-rule-evidence.md`。申报数量与部分成交
  股数分离；普通买入使用上海日期T+1，卖出FIFO分摊成本，最后一笔消耗剩余分。
- 成交要求逐项填写实际佣金、印花税、过户费和其他费；未自动估算或重复添加
  最低佣金。按真实股数/价格计算现金变动、剩余批次成本和卖出已实现盈亏。
- 成交事实、现金分录、批次、消耗关联、幂等回执与outbox在账户行锁内一次提交；
  永久交割编号防重，重复导入的新请求编号也保存回执。未来/倒序成交、现金不足、
  同日买入卖出和越权访问均拒绝，失败不留下部分账本。
- 增加两项迁移`cceb6410ca50`与`1f9cc1be43e0`，保留已有资金记录并回填
  成交请求回执。存在成交/回执时禁止破坏性降级；迁移校验通过。
- Web组合页加入成交表单、持仓表、分页成交记录和结算流水，所有金额及可卖
  股数来自后端。成交后刷新相关查询；持仓可进入股票研究。
- 后端18项测试通过，包含并发重试、37股部分成交、跨日FIFO、分批成本守恒、
  T+1和跨用户拒绝；Ruff、OpenAPI生成、TypeScript/Vite构建通过。
- 真实Chromium追加“37股×10元+5元实际费用→现金9424.99→刷新恢复→
  拒绝当日卖出”；串联已有账户/行情/材料流程，深浅四视口通过（5.7秒）。
  首次浏览器失败是合成资金时间带毫秒而成交时间截断为秒，触发倒序保护；
  将合成期初/出金改为明确前日日期后通过，没有放松账本时间校验。
- 限制：完整交易日历、历史证券身份、估算费用政策、冲正、期初持仓、
  公司行为、导入预览、独立对账和执行计划预留尚待实现；可卖为账本日期
  投影，不代表市场此刻开市或已满足全部执行条件。M2未完成，未写生产账户。

### 独立账本复算（T20 部分）

- 新增只读账户核对接口与页面操作，从原始成交价格/股数/费用及资金记录重新
  计算现金、FIFO批次成本和盈亏，并检查批次关联、时间/版本顺序及资金非负。
  不复用写入端的FIFO分摊函数，避免同一错误被双方共享而漏检。
- 报告列出具体记录、核对项、重算值与当前值；最多展示100条差异，报告保留
  实际差异总数。超过50,000条资金/成交记录明确拒绝即时核对，不输出部分成功。
  核对不自动修账；页面在账户版本变化后标记报告过期。
- 合成测试主动引入“多1股、多0.01元成本”两处投影漂移，独立重算准确检出；
  全部19项后端测试及Ruff/合同生成/构建通过。真实浏览器完成保存成交后的
  “核对账本→一致、1笔成交、1个未平批次、9424.99元”，完整场景5.3秒通过。
- 仅验证平台内部一致，未比对券商原始交割单、未覆盖公司行为和迁移，因此
  不将该结果称为真实账户已对账或迁移验收通过。

### 年度日历基础（T07 部分）

- 按三家交易所2025-12-22公告实现2026年度开休市和下一交易日查询，
  保留来源、版本、发布日期与保守可用时间，提供鉴权API及生成合同。
- 春节、国庆、中秋及工作调休周末测试通过；2025/2027未核验年份及公告前
  查询返回null/原因，禁止按工作日猜测。全部23项后端测试、Ruff、合同生成
  与构建通过。来源见`trading-rule-evidence.md`。
- 尚未接入临时停市、个股停牌、盘中时段和多年历史日历；不能把年度计划
  当作实时交易资格。现有历史成交录入仍按凭据事实处理，后续计划/回测使用
  日期资格时必须显式消费日历缺口。

### 整笔成交冲正（T16/T18/T20 部分）

- 新增预览、确认及分页冲正记录。账户行锁内验证版本与预览hash，保留原始
  成交，追加冲正事件及反向现金分录，原子重算FIFO批次、消耗和后续卖出盈亏。
  修改的盈亏仅为投影；原数量、价格、费用、时间及交割编号保持不变。
- 既有幂等回执已迁移为冻结返回值；冲正后的原交割编号仍不可重用。重复确认
  只产生一个冲正。核对已有漂移、后续卖出缺股、T+1不满足、历史现金不足或
  预览过期均拒绝；不会靠冲正静默修复已有漂移。
- 迁移`d3d6d6e07b60`已在本地升级；`alembic check`无漂移。后端27项测试通过，
  包括并发冲正、买入作废后卖出盈亏重算、原响应重试、现金依赖及越权。
  Ruff、生成合同、TypeScript/Vite构建通过。
- 真实Chromium完成“375元买入→预览冲正→现金9799.99→无持仓→刷新→
  原成交已冲正、原因可读→独立核对一致”，同时串联已有资金/研究流程，
  深浅四视口通过（6.0秒）；截图`test-results/correction-preview.png`本地可查。
- 当前仅整笔误录作废，价格/数量原子替换、资金冲正、倒序历史补录、
  导入预览和执行计划仍未完成。未迁移真实账户，未部署或切流。

### 交割单导入预览（T17 首批）

- 新增UTF-8统一CSV模板、500行/250KB浏览器限制、逐行字段/时间/费用校验、
  永久交割编号去重和持久化预览。预览在回滚保存点内运行真正的成交写入器，
  不残留成交、资金、批次、回执或outbox；有错误整批拒绝。
- 预览15分钟有效，绑定账户版本；确认使用预览ID永久防重，账户锁内重新核对
  后整批原子入账。支持重复文件只跳过重复成交，过期/版本变化/越权明确拒绝。
  前端URL保存账户和导入ID，刷新可继续核对预览。
- 迁移`9f1baf03940c`已升级、无声明漂移；全部30项后端测试、Ruff、生成合同、
  TypeScript/Vite构建通过。测试发现内部snake_case与外部camelCase版本键同时
  传入导致确认失败，已统一为校验后的内部字段更新并由并发导入用例覆盖。
- Chromium追加“上传→预览不变余额→刷新恢复→确认入账→重复文件再确认
  →余额不重复扣减→独立核对一致”，并串联已有冲正/研究、深浅四视口，
  完整场景6.1秒通过。仅本地合成账户。
- 不支持各券商原始表格自动映射、期初持仓或历史倒序补录；缺失/错误记录
  需要修正后重新预览，不能把部分入账冒充整文件成功。M2仍未完成。

### 人工计划与预留（T19 首批）

- 实现同日自主人工计划、数量/价格单位检查、现金及可卖数量原子预留、分页
  计划列表与显式原因取消。创建/取消写计划事件并推进账户版本，不写现金或持仓。
  账户核对按资金事件与计划事件共同验证版本，保留财务事实一笔一次。
- 真实成交可关联计划；部分成交消减剩余数量及费用预算，完成由真实成交推进。
  取消/过期后成交仍可补录；真实资金变动优先，覆盖不足的旧预留明确失效。
  已完成计划冲正后不会自动恢复原意图。
- 迁移`663f3b4aacad`本地升级且无声明漂移。35项后端测试通过，包括并发预留、
  部分成交/取消/晚到成交、卖出不释放现金、出金失效、完成后冲正与到期释放。
  Ruff、生成合同和TypeScript/Vite构建通过。
- Chromium串联“创建100股×10元+5元预算→预留1005元、账面余额不变→
  刷新→取消→可支配现金恢复→独立核对一致”，累计场景7.9秒、深浅四视口通过。
  部分成交关联已通过数据库集成测试，浏览器尚未覆盖该分支。
- 数量规则随上海实际运行日复核至2026-09-16，证据见交易规则文档。
  首批计划显式为自主账本意图，未校验实时行情/权限/市值仓位风险，不能称为
  系统可执行建议；费用为人工预算，到期为读取时派生状态、尚无后台到期审计。
  期初持仓、公司行为、完整原子更正和系统联合计划仍待实现，M2未完成。

### 批次独立身份（T14/T18 前置）

- 持仓批次独立主键，成交来源保留为可空唯一外键，卖出消耗直接关联批次。
  迁移沿用原ID及全部成本/数量，不制造成交或现金；后续非成交来源可独立接入。
- FIFO内核使用lot_id，冲正重建先恢复批次再恢复消耗；独立对账核对成交来源。
- 本地迁移、35项后端测试、Ruff与Alembic无漂移通过，既有API合同不变。
  此步仅数据边界调整，期初持仓录入及公司行动尚待后续实现。

### 期初持仓录入（T14/T18/T20 部分）

- 新增不可变期初批次事实与分页API/页面，记录取得日、基准时间、实际剩余
  总成本和唯一凭据。账户锁、版本、幂等回执、来源防重；有成交/计划后关闭期初。
  不扣现金、不伪造成交，卖出FIFO和冲正重建保留来源与精确成本。
- 独立对账加入期初事实、批次来源、版本及取得日期检查；修正混合锁定批次
  的核对逻辑，跳过当日锁定批次后仍能消费后方已结算批次。
- 迁移`8cd8d36e4cf7`升级，38项后端测试通过，Alembic无漂移；
  Ruff、OpenAPI/客户端生成、TypeScript/Vite构建通过。两项第三方弃用警告仍在。
- 真实Chromium新增“零现金→期初37股/375.01元→刷新→卖出12股→现金143元、
  剩余成本253.39元、实现盈亏21.38元→独立核对”，移动端截图检查通过。
  既有全串联场景及深浅四视口继续通过；两场景共11.1秒，仅合成账户且已清理。
- 尚无期初更正、转托管、公司行动或真实账户迁移；M2未完成，未部署或切流。

### 计划到期审计与关联成交验收（T19 增量）

- 独立账本维护Worker每秒扫描，到期计划在账户锁内释放剩余预留、追加EXPIRE
  审计并推进版本；并发Worker跳过已锁定账户，重复扫描不会重复写事件。
  到期读投影仍即时排除占用，不依赖Worker可用性；晚到成交保留事实而不复活预留。
- 计划轮询检测新账户版本后刷新余额版本，避免后台到期后表单继续使用旧版本。
- 39项后端测试、Ruff和TypeScript/Vite通过；合成时钟验证并发到期仅写一次、
  现金不变、晚到37股仍保留到期状态及独立核对一致。Worker本地已启动。
- Chromium增加关联计划部分成交：100股计划→记录37股→剩余预留630元→刷新
  →取消只释放剩余预留，现金9049.99元与持仓保留。两场景9.0秒通过，
  包含期初持仓以及既有深浅四视口/研究流程。尚未完成平台所有模块的全流程验收。

### 公司行动现金事实（T18 增量）

- 现金分红与实际红利补税独立类型，强制证券和永久凭据编号；不当作外部
  入出金、不推断税率、不改持仓成本或伪造成交。补税与出金一样受余额约束，
  冲突的计划预留按实际现金变化失效。
- 迁移`0a722e336341`修正Alembic约束名重复加前缀后升级成功；41项后端测试、
  Ruff、Alembic无漂移、生成合同及TypeScript/Vite构建通过。
- Chromium完成143元现金→分红12.34元→补税2.47元→刷新152.87元、
  剩余股份成本253.39元不变→独立对账；两个累计场景10.8秒通过。
  数据为合成凭据，实际派息资格/税率未自动验证，送转配股及公司行动更正仍待实现。

### 成交原子替换（T16/T18/T20 增量）

- 冲正预览/确认支持完整替换股数、价格及实际费用，保持原交割编号、证券、
  方向、时间及计划关系。原记录不可变，追加修订事实与新旧现金差额，
  在原发生顺序重算后续FIFO和盈亏；相关计划按更正股数重算剩余预留。
- 独立核对分别重算原始记录及修订值，未复用写入端更正计算。
  历史缺股、负现金、变更预览、跨账户访问均失败关闭；重复并发确认只记一次。
- 本地迁移0008成功且Alembic无漂移，43项累计后端测试通过，追加计划更正
  用例单独通过（共3项替换集成场景）；Ruff、合同生成及TypeScript/Vite通过。
- Chromium验证12股×12元卖出改为10股×13元，现金152.87→138.87、
  剩余27股/273.66元成本、盈亏27.65元，刷新后修订凭据可见、对账一致。
  两个累计场景11.0秒通过，仅本地合成账户。截图已检查。
- 限制：单笔一次更正；多次修订链、时间/方向更正、资金/期初更正与股份公司
  行动尚待实现。

### 长页键盘跳转入口修复

- 将跳转链接的屏幕外定位改为默认裁剪、聚焦显示，避免全页截图滚动拼接时
  显示负坐标的固定元素。保持Tab首焦点和Enter跳转能力。
- 浏览器验证默认隐藏→Tab显示→Enter跳转，累计两场景10.7秒通过；
  深浅四视口、移动端截图及TypeScript/Vite通过。

### 真实股份转入（T18/T20 增量）

- 新增独立转托管事实、批次来源与分页API/UI，保存原取得日、实际到账时间、
  剩余总成本和唯一凭据。已有成交后可转入，不扣现金、不伪造买入。
- 冲正把转入按原版本插入重算，未来转入不能覆盖历史缺股；独立对账验证
  全部事实时间顺序、三种批次来源和成本。转入后禁止追加期初。
- 迁移08e085a79bba本地升级、Alembic无漂移；46项后端测试、Ruff、
  生成合同及TypeScript/Vite通过。两个第三方弃用警告仍在。
- Chromium完成27股基础上转入13股/130.01元→刷新40股/403.67元成本，
  现金138.87元不变→独立核对一致；两个累计场景13.4秒通过，
  移动端截图及深浅四视口检查通过，仅合成账户。
- 当前只支持转入；转出、送转配股、非交易处置、资金/期初及多次更正链仍待
  实现，M2未完成。新模型、真实Agent、迁移与上线未完成。

### 新训练数据准备核查（T21 前置）

- 增加只读归档审计CLI，真实核查19个分片、2016-01-04至2026-09-11、
  10,114,448行（含跨片重叠），计算SHA-256并检测片内重复/非法OHLC/量额。
- 证券元数据5,901条，其中339条有退市日；各日线分片仅0～1只匹配退市证券，
  最新片3,051只均为沪深主板。历史身份、复权、分钟因果股票池及Agent证据
  缺口明确列入不可发布报告，没有把旧训练数组改名为新模型。
- 合成归档/只读SQLite集成验证通过，Ruff通过；无生产写入、未运行训练。
  结果与可复跑命令见`market-data-readiness.md`，机器报告在本地/tmp。

### 全量规范行情自身审计（T07/T08/T21 前置）

- v4 数据集已完成2016-01-01至2026-09-15全部2,601个开放日，包含5,812只
  历史证券、11,071,942条日线和11,259,492条复权因子；主板、创业板、科创板、
  北交所均在因果股票池中，退市证券未按当前目录剔除。
- 新增规范SQLite专用只读审计CLI，逐证券核对生命周期、开放日覆盖解释、
  OHLC/前收/量额十进制、复权、源代码有效期、可用时间、checkpoint和分钟聚合。
  11,259,562个应有证券开放日中，187,620个无日线日期全部有停牌或暂停上市
  解释，无未解释缺口；4个既有分钟分区均通过48时点和日线聚合复算。
- 完整库`integrity_check=ok`，数据库SHA-256和报告SHA-256已固化在
  `market-data-readiness.md`，报告无违规项。新增2项审计回归；市场数据相关
  54项测试通过。独立公开源扩展抽样仍在处理供应商冲突，因此T08尚未完成，
  当前结果也不代表分钟episode、标签、模型或收益已经验收。

### 扩展日线独立交叉核验（T08 前置）

- 公开源核验协议升级为v2，增加东方财富原始日K及成交额，要求每个样本至少
  一个独立来源完整确认，同时单列其他来源冲突。新增协议测试覆盖东方财富单位、
  非法响应、来源冲突和成交额不一致。
- 真实扩展到2016/2019/2021/2024/2026五个年份、四类股票17个高成交额样本：
  14个自动确认，3个保守失败。2016年`SH.600030`差异由上交所当时的收盘价
  VWAP规则解释；北交所2026样本的量额差异由同日4笔大宗交易逐笔闭合，
  不是竞价日线缺失。东方财富本机有3次协议断开，报告保留失败状态。
- 这次核验暴露了数据语义缺口：规范`daily`量额是竞价时段口径，部分公开日K
  包含大宗交易。大宗交易事实和口径字段尚未进入规范数据集，因此T08继续阻断，
  不开始标签训练。机器报告与具体差异见`market-data-readiness.md`。

### 大宗交易分账与规范数据集v5（T08/T21 前置）

- 数据合同升级为`market-dataset.v4`，日线量额明确为竞价口径，大宗交易按证券日
  独立聚合。Tushare原始万股/万元使用Decimal转为股/元，保留源行哈希和21:00
  保守可用时间；空交易日也有独立checkpoint。
- 同步按自然月批量请求，6,000行边界自动二分并保持单日恢复。B股、基金、债券、
  未知代码、北交所开市前新三板记录和代码复用期外记录分类审计；其他生命周期
  越界硬失败。真实运行发现并覆盖`302132.SZ`历史非股票代码复用边界。
- v5已封存全部2,601个开放日、152,629条证券日汇总和279,882笔A股大宗交易；
  连同11,071,942条日线、11,259,492条复权因子及4个分钟分区通过全库审计，
  无违规项。数据库与报告SHA-256记录于`market-data-readiness.md`。
- 后端125项测试与Ruff通过。该结果完成竞价/大宗交易事实分账，但候选episode
  分钟覆盖、标签、模型训练、全市场回测及联合Agent验证仍未完成，不据此发布。

### 全量候选episode分钟需求与归档迁移（T08/T21 前置）

- episode合同升级至v3。196,748个候选按执行日起5个开放日展开981,740条关联，
  去重为222,500个证券日：222,088个需分钟数据，412个因当日无规范日线明确标记
  `NOT_APPLICABLE`；末端4个决策日共400个episode因不足完整窗口延后。
- 分钟导入按源文件事务化，记录文件哈希、逐证券日接受/拒绝、聚合偏差和重试
  状态。每个可用证券日必须有48个标准端点，并通过OHLC、整数股、开收盘、日内
  范围、成交量100股及成交额5bps门禁；失败时不保留部分K线。
- 隔离归档首批248个交易日写入21,421个证券日；随后验证19个带逐文件SHA-256
  审计的2016-2026 Tushare分片，再写入56,041个证券日。当前合计完成77,462个
  证券日、3,718,176根K线。
- 仍有144,626个证券日待取；其中6,321个保留明确质量拒绝，原因包括开收盘、
  成交量、成交额或48时点不一致，其余约13.8万个未出现在旧归档。缺口集中在
  创业板、科创板和北交所，必须通过当前Tushare适配器补取，不能以旧Top1000
  股票池替代全类型覆盖。
- 正式episode SQLite完整性为`ok`，所有完成证券日均恰好48根且无空完成标记；
  后端新增Tushare分片身份与逐文件哈希回归；全量测试与Ruff通过。

### 当前Tushare分钟全量补取（T08/T21 前置）

- 新增按证券和最多120个规范开放日切窗的`fetch-minutes`阶段。单次最多请求
  5,760根5分钟K线，低于8,000行截断门禁；响应按需求证券日拆分，每日独立
  验证和提交，中断后从`PENDING`继续。
- 真实使用当前允许列表内的Tushare兼容端点跑完3,218个剩余窗口；原先约13.8万
  个从未出现于旧归档的缺口已全部尝试。四板和2016年历史分钟均验证可取，
  没有认证、限流或网络中断。
- 当前222,088个应取证券日中208,903个通过，共10,027,344根K线；13,185个
  保留质量拒绝。拒绝包括开收盘6,462、成交量3,306、会话缺失1,630、成交额
  1,340、源OHLC非法431、越出日线范围16；不存在未尝试或无原因的`PENDING`。
- 196,748个候选中174,891个已有完整五日路径，分板块为主板56,073、创业板
  61,340、科创板39,980、北交所17,498；21,857个候选仍被至少一个质量缺口
  阻断。上交所2018-08-20前历史收盘制度差异和北交所2022～2023供应商口径
  是不同问题，未用统一放宽阈值掩盖。
- SQLite`integrity_check=ok`，完成证券日总数乘48与分钟行数完全一致。新增
  封存门禁：存在任何`PENDING`时拒绝生成manifest，因此当前数据集未封存，
  T08/T21、标签、训练和回测仍未标记完成。

### 分钟质量规则分层与二次补取（T08/T21 前置）

- 根据上交所官方规则，股票收盘集合竞价自2018-08-20实施；此前官方日收盘价
  是最后一笔交易前一分钟成交量加权均价，不能要求其必然等于5分钟K线末笔价。
  摄取器现仅对`SH`且早于该日期的收盘差异应用例外，开盘、高低范围、量额和
  会话完整性仍全部校验；审计明确记录分钟末笔价、官方日收盘价，并规定终端
  退出使用规范日线收盘价。来源：
  https://www.sse.com.cn/aboutus/mediacenter/hotandd/c/c_20180806_4607055.shtml
- 成交量聚合由单一100股阈值改为严格小于
  `max(100股, 规范日成交量×0.05%)`，与成交额5bps门限对齐。该规则是在
  5,744个沪市历史拒绝上审计后冻结：5,737个为分钟合计少于日线，4,484个
  低于5bps，最大长尾超过1.2%，长尾未被放行。所有拒绝现在保存日线与分钟
  OHLC、量额、差值和实际容差；相同响应重试会幂等刷新审计详情。
- 补取器支持按拒绝原因恢复，并按交易日选择`instrument_aliases`中的有效
  源代码、在代码迁移边界拆窗。北交所旧`43/83/87`代码真实重试305个窗口，
  仅1个证券日通过，3,068个变为会话缺失；当前授权端点无法提供这些旧代码
  对应日期的完整分钟序列，不能用当前`920`代码下不一致的数据替代。
- 二次补取后，222,088个应取证券日中213,908个通过，共10,267,584根K线；
  8,180个仍为`PENDING`：会话缺失4,698、成交量1,621、开收盘920、
  成交额494、源OHLC非法431、越出日线范围16。完整五日路径177,711 /
  196,748，分板块为主板58,047、创业板61,384、科创板40,082、北交所
  18,198。SQLite `integrity_check=ok`；因外部分钟数据缺口仍未清零，
  数据集保持未封存，训练与全量回测不得宣称完成。

### 豆包搜索与Agent受控终止协议（T09/T10/T11 增量）

- Agent总墙钟预算为180秒，最多4次豆包搜索；任务租约210秒。搜索使用固定HTTPS
  端点和仓库外凭据，保存请求ID、时延、来源权威等级、发布时间、证据ID和工具轨迹，
  结果标记`SEARCH_DISCOVERED/SEARCH_RESULT_UNVERIFIED`，不等同于事实已核验。
- 最终研判必须调用`submit_assessment`，参数使用`AssessmentOutput` schema；自由文本
  即使看似合法JSON也拒绝。搜索与研判只能引用任务时点前的包内证据，外部文本仍按
  不可信输入处理。
- 真实只读探针中，极小终止工具15.6秒完成，完整研判工具8.1秒完成，生产函数的
  无搜索合成研判21.2秒完成并通过合同；带真实搜索的生产路径39.9秒完成，3次搜索
  共约2.1秒，接受5条证据并生成`UNCERTAIN`研判。探针没有数据库写入。
- `tests/test_research.py`4项及相关Ruff检查通过，覆盖强制终止工具、自由文本拒绝、
  搜索证据因果时点、工具预算、审计轨迹和fenced发布。历史长尾超时仍需重复运行
  评测；一次真实成功不代表供应商SLA或研判质量已经验收。

### 来源耗尽、数量条件标签与全市场排序（T21/T22/T23 增量）

- 原候选episode库已将213,908个证券日标记完成、412个不适用、8,180个在全部
  已知来源尝试后标记`SOURCE_EXHAUSTED`，无未决需求；封存后176,914 /
  196,748个episode可用。多规模标签包含一手、10万元参考及20日中位成交额
  0.05%/0.2%/1%场景，共877,518条，10万元不再是训练过滤条件。
- 全市场排序库覆盖2016-01-01至2026-09-15、5,812只历史证券和10,223,999条
  点时样本；主板6,953,408、创业板2,354,483、科创板683,118、北交所232,990。
  数据库SHA-256为`b2338d8dcdeeede73587ee7068a3a5d095daabacbaa4dfe0d64ca0d2f39a759d`。
- 排序模型确认集为2024-08-13至2026-09-08，共2,648,931样本/503日；
  平均日RankIC 0.0991，Top10五日毛收益均值0.9844%，10bps粗压力后0.8844%，
  四板均有入选。该指标未使用分钟成交、账户资金或Agent，不是可发布收益。
- corrected enriched v2统一消费排序模型同源31维全市场特征，删除语义不一致的
  旧流动性Top25名次。相对初始基线，确认集`pFill` AUC 0.5455→0.5741、
  `pFullFill` 0.7583→0.7757、`pWinGivenFill` 0.5303→0.5420、
  `stopHazard` 0.5776→0.5987，Q10-Q90覆盖率71.35%→73.85%；收益MAE未改善。

### 全市场选择的分钟执行回测（T23/T37 前置）

- 首次覆盖审计证明旧流动性Top25标签只能覆盖全市场模型5,030个时间外Top10
  选择中的100个（1.99%），因此未将旧标签冒充全市场执行回测。
- 为全部5,030个选择建立独立episode库，18,452个唯一证券日经两个允许的
  Tushare兼容端点尝试；17,776个通过严格质量门禁，676个有至少两次尝试后
  标记`SOURCE_EXHAUSTED`。最终4,583个episode可用、447个不可用，覆盖率91.11%；
  四板可用数为主板2,666、创业板1,281、科创板554、北交所82。
- 新标签库含22,840个数量场景、20,324个至少成交一手场景。episode数据库
  SHA-256为`9747abd668aaf931ce0c863437e003cd18fd1518b9a8961ca34a32abfc261e15`，
  标签数据库SHA-256为`b5138c79fc1a919c7cab8fe74e8c5a23746b83d5dca68de2432d8c53f5f88961`。
- 排名Top10的4,583个可用路径在10bps附加压力后平均为-0.0886%。预先冻结的
  模型门禁`pFill × (expectedNetReturnGivenFill - 10bps) > 0`保留1,073条，
  其中925条成交，压力后平均+0.2739%，预测与实际相关性0.159。
- 结果不稳定：科创板压力后均值-0.7424%，2025年-0.0533%，预测效用五分位
  非单调；另有8.89%选择因可信分钟数据缺失不可评估。报告保持`UNAVAILABLE`，
  阻断项包括覆盖不足、板块/时期失败、单调性失败、账户资金回放、Agent包和
  联合消融。该报告是重叠的独立episode诊断，不是账户净值或已验证收益。

### 多账户规模资金回放（T29/T37 增量）

- 使用同一模型门禁按10万、50万、100万、500万元初始现金回放，最多10个并发
  持仓；每个槽位使用初始现金10%，高价股按一手保留，只有真实现金不足才跳过。
  计划先预留目标成本，账本只扣实际部分成交；同股重叠跳过，买入在同日退出
  之前处理，持仓按规范日线收盘价盯市，停牌沿用最近价格。
- 额外10bps压力后，四种资金规模总收益分别为+3.73%、+2.02%、+0.42%和
  -7.86%；压力最大回撤分别为10.85%、10.52%、10.69%和12.92%。500万元
  场景暴露明显容量外推失败，不能把小账户正收益线性放大。
- 四个资金场景中科创板均亏损，2025年均亏损；500万元下创业板也转负。
  账户报告SHA-256为
  `219dc83c98d3f3b18ea5652bd561cf1c9e162340870ca3564b0ad573aac8dd3e`，
  发布继续被账户规模、板块、时期、覆盖和联合消融阻断。

### 持仓配对动作模型（T26/T27 增量）

- `position-action-label.v1`在同一持仓状态和未来路径下比较HOLD、ADD、REDUCE、
  EXIT；动作从入场后下一交易日前30分钟开始，按每bar 5%参与率、100股离散数量、
  历史费用和5bps滑点执行，未将EXIT定义为买入收益取负。
- 封存630,311个多数量持仓状态，覆盖150,515个episode和四板；无约束最佳动作
  为ADD 278,659、EXIT 337,924、HOLD 13,018、REDUCE 710。REDUCE虽低频，但
  因容量和分笔费用的非线性存在真实监督信号。数据库SHA-256为
  `0ba2a8280e4ec88761112ebaea6ac72f998caa4a8a1c6d4f55ae0051665212a3`。
- 三个回归头预测ADD/REDUCE/EXIT相对HOLD增量，由组合约束仲裁动作。确认集
  169,679个状态，平均regret 6.29%、P90 regret 18.41%；模型选择动作相对HOLD
  平均-0.0371%，主板、创业板、科创板均为负，仅北交所接近零。
  artifact SHA-256为
  `a79f5f5d9df927bab5b002d2a808a913d066639184dde2d9481e2872d46f007a`，
  因无正增益、板块失败、REDUCE支持低及缺Agent联合回放保持`UNAVAILABLE`。

### Agent联合候选包（T25/T40 前置）

- `agent-features.v1`只编码已校验研判的论点状态、策略适配、事实/推断/假设计数、
  反证、不确定性和证据数，不把自然语言临时映射成交易分数。Prompt、工具schema、
  Agent模型和`research-assessment.v1`协议均以SHA/版本绑定。
- `joint-candidate-v2`绑定排序模型、enriched执行模型、账户回放和持仓动作模型
  artifact；缺失项只剩前瞻Agent特征数据集与训练后的联合模型。历史Agent结果
  不使用今天的LLM记忆伪造，须从现在开始积累前瞻样本。
- 联合包默认加载返回`JOINT_BUNDLE_NOT_RELEASED`，研究模式只能得到
  `UNAVAILABLE/NONE`且`allowsNewRisk=false`。manifest SHA-256为
  `66908290d57a0ded94c4ab350509bf514ac12c93ad78b4089f8bd945c24b456d`；
  当前未完成联合模型训练、Agent质量评测、联合/去Agent/去量化消融，因此不能发布。

### 联合持仓运行时仲裁（T25/T26 增量）

- 新增`position-action-reference.v1`，要求量化侧同时给出四动作相对HOLD的
  期望增量、Q10/Q50/Q90、止损风险、样本支持、趋势、期限、费用与校准引用；
  HOLD必须保持当前数量并使用零增量基线。现有只输出均值的持仓模型不满足完整
  生产合同，不能通过缺省值伪装。
- 新增`position-assessment.v1`，持仓Agent只提交结构化论点、反证、不确定性及
  来源信号；主力资金和订单流明确保存供应商、推导口径、发布时间、首次获取时间、
  可用时间和校验状态，不将供应商推导指标写成已证实的“主力身份”事实。
- 确定性仲裁器按硬风险、联合发布门禁、模型/Agent组件绑定、DecisionContext、
  时点隔离、账户数量约束的顺序处理。硬止损不等待模型或Agent，按当前可卖数量
  输出REDUCE/EXIT；无可卖数量保留风险阻断。普通动作必须来自同一READY联合版本；
  Agent与量化冲突、任一输入缺失/过期或未来证据均返回`UNAVAILABLE/NONE`，
  不冒充HOLD。
- `JointBundle`现绑定持仓Agent协议并校验运行时release引用；READY manifest若仍有
  blocker、缺失artifact或协议不完整会被拒绝。当前`joint-candidate-v2`仍因前瞻
  Agent样本、联合训练及消融缺失保持`UNAVAILABLE`。
- 聚焦与全量验证：`uv run pytest -q`为204 passed；`uv run ruff check src tests`
  通过。尚未实现Position Agent任务持久化、前瞻样本采集和完整联合模型训练，
  因此本增量只完成可审计运行时合同与仲裁链路，不代表AC08或M3验收完成。

### 联合架构影子发布与每日评估（T25/T30/T40/T41/T46 增量）

- 用户明确要求直接发布Agent+量化架构并按日回测迭代。当前候选不满足实盘收益
  门禁，因此发布为正式`SHADOW`模式而非伪造`READY`：
  `joint-shadow-20260916-v1`已原子写入仓库外
  `~/.local/share/stock-platform/joint-releases/active-shadow.json`。发布时重新校验
  ranking、quant、position artifact及账户回放SHA；活动指针绑定不可变manifest，
  `allowsNewRisk=false`。
- SHADOW允许模拟账户运行并保存唯一动作，真实账户固定返回不可用且不能据此创建
  新增风险计划。持仓页面展示活动版本、影子状态、门禁数、量化趋势、Agent论点、
  唯一动作及阻断原因；任务和决策刷新后从服务端恢复。
- 影子量化运行时不读取含未来结果的训练行。它从封存市场库截至决策时点重新计算
  当日全市场横截面特征，使用发布包绑定的ranking和position artifact；2026-09-16
  探针实际使用2026-09-15行情，官方年度交易日历给出的第五交易日为2026-09-22。
  当前position artifact只提供动作均值，非HOLD动作的Q10/Q50/Q90、stopHazard和
  support明确标记缺失；READY模式会拒绝这些不完整预测。
- 有效`research-assessment.v1`可规范化为影子`position-assessment.v1`，保留原
  evidence、发布时间、首次获取和可用时间；主力资金、订单流和实时交易缺失被写入
  uncertainties，不以零值代替。完整影子评估会写入`prospective_samples`，等待
  5交易日结果成熟后用于联合训练和消融。
- 首次每日周期
  `daily-20260916T105434Z`已运行并保存于仓库外
  `~/.local/share/stock-platform/daily-joint-cycles/`。10万/50万/100万元压力收益
  分别为+3.73%/+2.02%/+0.42%，500万元为-7.86%；分钟覆盖91.11%，成熟前瞻Agent
  样本0，因此结论为`KEEP_CURRENT_RELEASE`。每日运行不等于每日强制升级，只有
  所有冻结门禁通过才生成晋级候选。
- 恢复缺失的前瞻样本迁移revision `e4a9c6f87d10`和ORM；既有数据库无业务行被
  重写，新建临时数据库从base升级到head共25张表通过，随后决策发布迁移
  `0010_decision_publication`上线。当前全量验证为211 passed，Ruff、Alembic
  drift、TypeScript与生产构建通过。尚未完成Position Agent独立前瞻运行、
  样本成熟结算、四组联合消融及实盘READY晋级。

### 活动影子包真实运行时验收（T25/T30 增量）

- 直接使用活动指针`joint-shadow-20260916-v1`、封存市场库和真实
  `position-action-model-v2`运行本地PostgreSQL完整链路；未向请求注入量化值。
  合成模拟账户上的有效研究研判生成`READY/REDUCE`，并持久化
  `DecisionContext`、唯一当前`Decision`和`PENDING`前瞻样本。样本绑定
  `a-share-20160101-20260915-v5:20260915`市场快照，探针结束后已验证删除全部
  合成账户、证据、任务、决策和样本。
- 性能剖析确认热请求约5至7秒耗在每次扫描4.5GB封存SQLite查询最近交易日，
  模型推理约27毫秒。最近交易日按封存库路径和候选日期缓存后，同进程同交易日
  复用从约5.6秒降至约66毫秒；首次进程仍需约26至45秒完成工件hash校验和
  全市场特征截面构建。首次慢启动必须由部署预热处理，不能在API请求中隐瞒。
- 持仓决策聚焦测试更新为15项并通过，Ruff通过。本验收证明影子运行工程链路
  可用，不改变`allowsNewRisk=false`，也不代表联合收益门禁或实盘READY通过。

### 决策到执行计划关联（T34 增量）

- `READY`且仍为账户当前指针的交易动作可由用户显式确认生成执行计划；
  `ADD`映射买入，`REDUCE/EXIT`映射卖出，数量、价格边界、费用预算和有效期均
  取自已发布决策。`HOLD`、过期、旧指针、账户版本变化或执行合同缺失会拒绝。
- `execution_plans`新增唯一`decision_id`与`source`，系统计划固定标识
  `SYSTEM_DECISION/JOINT_DECISION_PLAN`。同一决策重复提交只返回原计划；
  计划确认只创建预留，不修改现金或持仓，后续实际成交仍由人工录入并沿用既有
  部分成交、取消和冲正规则。
- 持仓建议表仅对可执行动作显示“生成执行计划”，计划表展示联合决策来源。
  Alembic升级至`0011_decision_plan_link`且无schema漂移；相关后端21项测试、
  TypeScript检查和生产构建通过。

每个切片记录实际修改、验证命令、结果、限制和提交，不记录密钥、完整私人账户或无关日志。只有实际通过的任务才更新完成状态。
