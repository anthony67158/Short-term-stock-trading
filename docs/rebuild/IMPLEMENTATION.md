# 重建实施记录

## 授权与目标

用户已在2026-09-15明确要求开始全量重建与替换，必要时重新训练A股量化模型，系统完成后通过browser-use跑全部流程并修复。按`a-share-platform-rebuild.v1.0`执行；选股与持仓管理必须由量化模型和Agent共同参与。

Agent服务指定为`https://linlongs.com`，实际API模型ID为`gpt-5.6-terra`。180秒预算下真实只读生产路径已生成并通过本地合同校验；供应商仍存在历史尾延迟，研判质量和重复运行稳定性尚未验收，禁止擅自替换模型或伪造结果。凭据保存在仓库外私密配置，任何提交、日志和浏览器不得包含明文。

## M0恢复点

- 初始提交：`7c153939f120ca16bda83d1fd2de6a548d300767`。
- 原工作区带大量未提交增删改，已保存tracked/untracked快照、删除清单、binary patch和状态。
- 私密恢复目录：`/Users/bytedance/Desktop/stock-dashboard-recovery-20260915`，目录权限700。
- 压缩快照包含1,313个文件，已逐文件读回并核验SHA-256。
- 旧环境配置与凭据独立保存在恢复目录private下，文件权限600；未写入Git。
- 重建分支：`rebuild/platform-v1`，在共享工作区创建；旧业务树已隔离归档，清理提交`d6d5d5c6`。
- 私密运行配置：`/Users/bytedance/.config/stock-platform/platform.env`，权限600；Agent与豆包搜索凭据均未进入仓库。

## 环境探测

本机已有Node22、pnpm10、uv。已安装PostgreSQL17.11，并在仓库外建立独立本地集群，
仅监听127.0.0.1:55432，TCP采用SCRAM认证。数据库与秘密配置在
`~/.config/stock-platform/`；新应用只加载`platform.env`，不自动加载旧`.env`。
Python3.12及依赖由`backend/uv.lock`锁定。云端已建立按量Serverless PostgreSQL
和FC 3.0只读SHADOW运行时；真实账户迁移、外部密钥注入和完整成本观察仍未完成。

## 当前进度

| 任务 | 状态 | 证据/下一步 |
|---|---|---|
| T00规格与追溯 | 已完成设计核验，已获实施授权 | 文档结构、链接、55项任务依赖与20组AC检查通过 |
| T01恢复点 | 已完成备份及清理清单 | recovery目录`cleanup-plan.json`与`legacy-tree/`保存106项移出资产；包括ignored数据、模型、资料，无资产销毁 |
| T02新工程 | 后端与Web独立启动、构建通过 | 根AGENTS/README重写；新工程不引用旧服务 |
| T03基础合同 | 已实现首批并生成客户端 | Decimal字符串、严格股数、UTC时间、错误包；`pnpm contracts`通过，随业务扩展 |
| T04 UI基础 | 深浅主题、实际工作区和响应式验收完成 | 部署端真实Chromium覆盖390/768/1280/1440、深浅主题、键盘与长页；TRAE browser-use桥接工具在当前会话不可用，使用Playwright回退 |
| T05运行验证 | 本机与云端只读SHADOW链路已验证 | API/Web及4类Worker在本机8080和FC运行；Alembic、租约、备份、恢复及云端健康检查通过 |
| T06身份 | 会话及基础安全已实现 | Argon2、HttpOnly cookie、注销/过期、Origin校验、共享限流；账户级授权随后扩展 |
| Agent连通性 | 真实只读链路可完成，质量与稳定性待验收 | `gpt-5.6-terra`在180秒预算内完成搜索规划、3次豆包检索和结构化研判；历史尾延迟仍保留为风险 |
| 应用/模型/全流程验收 | 原工作台已恢复，当前模型按用户授权直用并完成生产只读验收 | OSS仍是唯一交易事实源；设备授权已移除；模型保持`productionEligible=false`并明确标记实验直用，不自动写交易事实 |

### 首批验证（2026-09-15）

- `uv run alembic upgrade head`：0001任务/outbox、0002身份迁移成功。
- `uv run platform-cli health`：实际PostgreSQL连接成功。
- `pytest -q backend/tests`：6项通过，覆盖金额/股数、并发幂等、租约fencing、
  取消发布、会话/注销/过期、Origin与登录限流；存在2项第三方弃用警告，后续锁定兼容版本处理。
- `pnpm contracts`：实际生成OpenAPI与TS；`pnpm build`包含TypeScript检查并构建成功。
- `pnpm lint`：后端Ruff通过（前端lint后续补齐）。
- browser-use发现并修复CSS token导入层级错误；实际页面标题、登录form和深色背景正确，
  472px视口无横向溢出。发现浏览器恢复到旧生产域名后立即关闭，未执行任何生产写入。
- 框架依据：FastAPI multiple files、SQLAlchemy2 Session Basics、openapi-ts/openapi-fetch官方文档。

## 后续交付记录

### 账户资金切片（T13/T16/T18 的首批范围）

- 实现账户创建、实盘/模拟隔离、显式单股风险上限、期初/入金/出金、分页流水。
  PostgreSQL迁移0003包含账户归属、唯一幂等键、版本与资金符号约束。
- 资金写入锁定账户，校验版本与归属；重复请求返回原记录，内容变化返回409；
  不允许未来资金事实、超额出金、重复期初；按发生顺序录入。
  金额采用Decimal；账本更新和`portfolio.changed` outbox同事务提交。
- Web组合与执行页面接入生成的API类型，支持创建、选择账户、资金录入、
  错误反馈、流水分页和刷新恢复。纯现金余额不冒充净资产或可用资金。
- `uv run pytest -q`：8项通过（含并发重复写、精确金额、跨账户访问、错误金额、
  版本与余额约束）；`alembic check`确认数据库与声明无漂移；构建和Ruff通过。
- `pnpm test:e2e`：真实Chromium完成创建→期初10,000.01→刷新→拒绝超额出金
  →出金200.02→余额9,799.99→登出→刷新，1条完整场景通过。
  同场景检查390/768/1280/1440深浅主题无页面横向溢出，保留8张本地截图。
- 浏览器发现并修复：长用户名挤出移动端按钮；清空整个QueryCache导致登出后
  活跃observer仍展示私人页面。新逻辑先更新会话再移除其他查询。
- browser-use MCP已验证本地登录页面；该工具禁止代填密码。账户内流程通过
  Playwright调用真实登录接口建立临时合成会话后执行，测试结束清理合成账户。
  这不代表全部平台流程已完成browser-use验收。
- 限制：证券、持仓、成交、预留、冲正、公司行为尚未进入本切片，M2未完成；
  本地验证不代表生产部署。两条第三方依赖弃用警告仍待处理。

### 当前证券目录与报价切片（T07/T08/T12 部分）

- 独立实现新浪当前A股目录与腾讯报价Adapter，没有调用旧业务服务。
  东方财富目录探测连接失败，未作为必要依赖。行情格式为实际端点采样验证，
  尚无商业授权/SLA，不声称具备机构历史行情完整性。
- `uv run platform-cli sync-instruments`真实下载5,561只证券；56页按symbol排序，
  逐页数量、全局唯一、头尾总数检查通过才原子发布。保存成员快照及SHA-256，
  未收录证券仅标记不在当前目录，不擅自判定退市。首次观察时间不冒充上市时间。
- 证券代码保留前导零与交易市场；目录明确`CURRENT_ONLY`。研究报价保存行情时间、
  接收时间、来源、缺失与过期状态；所有报价暂不具备执行资格。
- Web市场支持搜索、分页、关注列表；股票详情展示报价和来源时间、
  加入/移出关注。尚无Agent论点或联合预测时明确展示缺口。
- `uv run pytest -q`10项通过；`alembic check`无漂移；OpenAPI客户端生成、
  TypeScript与Vite构建、Ruff通过。
- 浏览器场景追加真实搜索000001→平安银行→读取报价→加入关注→返回列表
  →刷新恢复→移出关注，完整场景通过（4.5秒）。临时测试身份和私有数据自动清理。
- 限制：历史证券身份、交易日历、日线、财务与公告尚未实现；
  公开源当前快照不能充当历史因果股票池，T07/T08/T12未全部完成。

### 规范历史行情数据集（T07/T08/T21 前置）

- 新增仓库外、可恢复、封存后不可写的SQLite数据集构建器和CLI；按证券参考、
  交易日分区和最终封存执行。重复源行幂等，内容冲突、重复键、6000行截断风险、
  复权因子缺失及无停牌解释的行情缺口均硬失败。
- 证券目录覆盖上市、暂停和退市状态，按请求日期区间构造因果股票池。北交所
  旧代码通过映射表归入稳定920身份，保留源代码与原上市日；A股池最早从
  2021-11-15北交所开市日起纳入。
- 真实2026-09-15探测封存5,562只证券、5,548条日线、5,562条复权因子和
  17条停复牌事实，覆盖主板、创业板、科创板、北交所。SQLite完整性检查通过，
  manifest数据库SHA-256独立重算一致。
- 后端65项测试及Ruff通过，新增覆盖批量冲突回滚、恢复重放、封存不可写、
  因果上市/退市范围、停牌解释、北交所映射、上游截断和仓库外路径限制。
- 当前仅完成单日真实验收；2016年至今全量下载、曾用名、全市场分钟策略、
  独立公开源交叉核验、标签重构、训练及回测尚未完成，不能标记T07/T08通过。
- 增量加入3条交易所公告佐证的证券代码迁移，以及12段暂停上市期间。
  同主体新旧代码仅在逐值一致时按当日有效代码保留；北交所上市资格前的新三板
  回填、退市后延续的复权因子按证券生命周期分类丢弃，数量和源行SHA-256进入
  checkpoint。其他证券或流类型越界继续硬失败。
- 新建仓库外v4数据集，真实完成2016-01-04、2020-01-02、2021-11-15三个
  边界开放日，累计10,895条日线、11,189条复权因子和323条停牌记录；
  SQLite完整性为`ok`。三日分别验证北交所开市前、退市后因子延续、开市日
  未来北交所证券回填，长期无日线证券均由官方暂停上市期间闭合。
- 本增量后端全量72项测试和Ruff通过。全仓`ruff format --check`仍报告74个
  历史文件未格式化，本切片只格式化改动文件，未制造无关迁移和测试改写。
  全开放日下载尚未完成，因此T07/T08仍不标记通过。
- 全开放日下载推进中在2016-09-06发现`SZ.300372`缺口。深交所公告确认其
  当日起暂停上市、2017-07-17进入退市整理期；补充官方区间和回归测试后该日
  真实分区通过。当前连续checkpoint已推进至2017-04-10，仍未完成全区间。

### 按候选证券获取5分钟数据（T08/T21 前置）

- 新增显式`minute`构建阶段，只允许传入唯一、合法的证券ID。每个证券日必须
  先有已验证日线分区和当日日线，不提供全市场无界分钟下载入口。
- 写入前严格检查48个标准5分钟时点、稳定证券身份、开收盘、日内价格范围、
  成交量小于100股偏差，以及成交额2元或5bps门限；聚合值、实际偏差、门限和
  日线源行hash进入checkpoint。任一失败不写分钟行，不插值、不改写日线。
- 北交所分钟接口真实要求当前稳定代码，不能沿用日线历史旧代码；构建器保留
  两类源代码并映射到同一稳定身份，已有合成回归覆盖。
- 真实写入2021-11-15主板/创业板/科创板和2022-11-15北交所四个样本分区，
  共192根K线，SQLite完整性为`ok`。北交所开市首日虽返回48根，但成交额偏差
  约10.03bps，超过门限后被拒绝且零写入。
- 后端累计78项测试及Ruff通过。该切片只证明四类股票可按需获取和严格验收；
  候选episode分钟覆盖率尚未随回测生成，T08/T21仍不标记完成。

### 独立公开源日线交叉核验（T08 前置）

- 新增新浪与腾讯历史日K严格解析器及只读交叉审计CLI，报告逐样本保存源可用性、
  OHLC比较、成交量差、源行hash和总报告hash，不把公开端点作为写入事实源。
- 真实核验2022-11-15与2026-09-15四板块共8个样本全部通过。新浪8/8一致；
  腾讯沪深6/6一致，成交量差0～37股；腾讯北交所历史行缺失被明确记录。
- 探测发现腾讯科创板volume已是股，而沪深主板/创业板为手；按板块修正单位并
  增加协议测试，没有用放宽容差掩盖百倍单位错误。报告与hash见
  `market-data-readiness.md`。
- 该结果是跨年份分层抽样，不是全库逐行双源验证；两个公开源也未独立提供本次
  成交额核验。全量规范库完成前T08仍不标记通过。

### 证据与研究运行器切片（T09/T10/T11 部分）

- 手工原文入库、HTTPS来源与发布时间、逐字引用校验、内容哈希、用户隔离、
  不可变修订；`USER_SUPPLIED/QUOTE_MATCHED`明确区分用户材料与已核验外部事实。
  引用匹配不表示原文真实；目前不自动抓取用户URL。
- Research任务冻结材料包、问题、asOf、模型、协议与截止；预算按用户及全平台
  24小时调用次数限制，单包60,000字符、最多16份材料、单次输出2,500 token。
  服务默认未启用，避免反复请求已知鉴权失败的端点。
- 独立Worker以PG租约领取；调用前标记外部步骤，单次HTTP调用含墙钟截止与
  最大响应体限制。结构化输出拒绝越权字段、包外引用、无法逐字定位的OBSERVED、
  无效期限。事实/推断/假设分型保存，所有研判仍是RESEARCH_ONLY。
- 研判、任务终态和outbox在同一fenced事务提交；取消发生在调用期间时不保存研判。
  前端提供材料选择、研究问题、实际任务状态、停止与论点版本列表，任务ID保存在
  URL用于刷新恢复。Agent不可用时按钮禁用并显示具体原因。
- `uv run pytest -q`13项通过，含合成HTTP协议、非法引用、生成中取消、
  不可变原文及跨用户读取。模型成功测试使用合成响应，未调用真实供应商。
  TypeScript/Vite构建通过；浏览器追加材料保存→刷新→引用展开→选择材料
  →研究不可用状态，完整场景通过（5.6秒）。
- 限制：自动公告/财报原文采集、多角色研究、完整论点版本编辑、
  历史研判与联合特征尚未实现；搜索结果仍是待核验发现线索。
  测试通过不代表真实研判质量已验收，M1尚未完成。

### 申报数量与成交账本切片（T14/T15/T16/T18 部分）

- 新建纯Python交易内核：按沪深主板、科创、创业板、北交区分限价数量；
  生效/核验区间与官方来源见`trading-rule-evidence.md`。申报数量与部分成交
  股数分离；普通买入使用上海日期T+1，卖出FIFO分摊成本，最后一笔消耗剩余分。
- 成交要求逐项填写实际佣金、印花税、过户费和其他费；未自动估算或重复添加
  最低佣金。按真实股数/价格计算现金变动、剩余批次成本和卖出已实现盈亏。
- 成交事实、现金分录、批次、消耗关联、幂等回执与outbox在账户行锁内一次提交；
  永久交割编号防重，重复导入的新请求编号也保存回执。未来/倒序成交、现金不足、
  同日买入卖出和越权访问均拒绝，失败不留下部分账本。
- 增加两项迁移`cceb6410ca50`与`1f9cc1be43e0`，保留已有资金记录并回填
  成交请求回执。存在成交/回执时禁止破坏性降级；迁移校验通过。
- Web组合页加入成交表单、持仓表、分页成交记录和结算流水，所有金额及可卖
  股数来自后端。成交后刷新相关查询；持仓可进入股票研究。
- 后端18项测试通过，包含并发重试、37股部分成交、跨日FIFO、分批成本守恒、
  T+1和跨用户拒绝；Ruff、OpenAPI生成、TypeScript/Vite构建通过。
- 真实Chromium追加“37股×10元+5元实际费用→现金9424.99→刷新恢复→
  拒绝当日卖出”；串联已有账户/行情/材料流程，深浅四视口通过（5.7秒）。
  首次浏览器失败是合成资金时间带毫秒而成交时间截断为秒，触发倒序保护；
  将合成期初/出金改为明确前日日期后通过，没有放松账本时间校验。
- 限制：完整交易日历、历史证券身份、估算费用政策、冲正、期初持仓、
  公司行为、导入预览、独立对账和执行计划预留尚待实现；可卖为账本日期
  投影，不代表市场此刻开市或已满足全部执行条件。M2未完成，未写生产账户。

### 独立账本复算（T20 部分）

- 新增只读账户核对接口与页面操作，从原始成交价格/股数/费用及资金记录重新
  计算现金、FIFO批次成本和盈亏，并检查批次关联、时间/版本顺序及资金非负。
  不复用写入端的FIFO分摊函数，避免同一错误被双方共享而漏检。
- 报告列出具体记录、核对项、重算值与当前值；最多展示100条差异，报告保留
  实际差异总数。超过50,000条资金/成交记录明确拒绝即时核对，不输出部分成功。
  核对不自动修账；页面在账户版本变化后标记报告过期。
- 合成测试主动引入“多1股、多0.01元成本”两处投影漂移，独立重算准确检出；
  全部19项后端测试及Ruff/合同生成/构建通过。真实浏览器完成保存成交后的
  “核对账本→一致、1笔成交、1个未平批次、9424.99元”，完整场景5.3秒通过。
- 仅验证平台内部一致，未比对券商原始交割单、未覆盖公司行为和迁移，因此
  不将该结果称为真实账户已对账或迁移验收通过。

### 年度日历基础（T07 部分）

- 按三家交易所2025-12-22公告实现2026年度开休市和下一交易日查询，
  保留来源、版本、发布日期与保守可用时间，提供鉴权API及生成合同。
- 春节、国庆、中秋及工作调休周末测试通过；2025/2027未核验年份及公告前
  查询返回null/原因，禁止按工作日猜测。全部23项后端测试、Ruff、合同生成
  与构建通过。来源见`trading-rule-evidence.md`。
- 尚未接入临时停市、个股停牌、盘中时段和多年历史日历；不能把年度计划
  当作实时交易资格。现有历史成交录入仍按凭据事实处理，后续计划/回测使用
  日期资格时必须显式消费日历缺口。

### 整笔成交冲正（T16/T18/T20 部分）

- 新增预览、确认及分页冲正记录。账户行锁内验证版本与预览hash，保留原始
  成交，追加冲正事件及反向现金分录，原子重算FIFO批次、消耗和后续卖出盈亏。
  修改的盈亏仅为投影；原数量、价格、费用、时间及交割编号保持不变。
- 既有幂等回执已迁移为冻结返回值；冲正后的原交割编号仍不可重用。重复确认
  只产生一个冲正。核对已有漂移、后续卖出缺股、T+1不满足、历史现金不足或
  预览过期均拒绝；不会靠冲正静默修复已有漂移。
- 迁移`d3d6d6e07b60`已在本地升级；`alembic check`无漂移。后端27项测试通过，
  包括并发冲正、买入作废后卖出盈亏重算、原响应重试、现金依赖及越权。
  Ruff、生成合同、TypeScript/Vite构建通过。
- 真实Chromium完成“375元买入→预览冲正→现金9799.99→无持仓→刷新→
  原成交已冲正、原因可读→独立核对一致”，同时串联已有资金/研究流程，
  深浅四视口通过（6.0秒）；截图`test-results/correction-preview.png`本地可查。
- 当前仅整笔误录作废，价格/数量原子替换、资金冲正、倒序历史补录、
  导入预览和执行计划仍未完成。未迁移真实账户，未部署或切流。

### 交割单导入预览（T17 首批）

- 新增UTF-8统一CSV模板、500行/250KB浏览器限制、逐行字段/时间/费用校验、
  永久交割编号去重和持久化预览。预览在回滚保存点内运行真正的成交写入器，
  不残留成交、资金、批次、回执或outbox；有错误整批拒绝。
- 预览15分钟有效，绑定账户版本；确认使用预览ID永久防重，账户锁内重新核对
  后整批原子入账。支持重复文件只跳过重复成交，过期/版本变化/越权明确拒绝。
  前端URL保存账户和导入ID，刷新可继续核对预览。
- 迁移`9f1baf03940c`已升级、无声明漂移；全部30项后端测试、Ruff、生成合同、
  TypeScript/Vite构建通过。测试发现内部snake_case与外部camelCase版本键同时
  传入导致确认失败，已统一为校验后的内部字段更新并由并发导入用例覆盖。
- Chromium追加“上传→预览不变余额→刷新恢复→确认入账→重复文件再确认
  →余额不重复扣减→独立核对一致”，并串联已有冲正/研究、深浅四视口，
  完整场景6.1秒通过。仅本地合成账户。
- 不支持各券商原始表格自动映射、期初持仓或历史倒序补录；缺失/错误记录
  需要修正后重新预览，不能把部分入账冒充整文件成功。M2仍未完成。

### 人工计划与预留（T19 首批）

- 实现同日自主人工计划、数量/价格单位检查、现金及可卖数量原子预留、分页
  计划列表与显式原因取消。创建/取消写计划事件并推进账户版本，不写现金或持仓。
  账户核对按资金事件与计划事件共同验证版本，保留财务事实一笔一次。
- 真实成交可关联计划；部分成交消减剩余数量及费用预算，完成由真实成交推进。
  取消/过期后成交仍可补录；真实资金变动优先，覆盖不足的旧预留明确失效。
  已完成计划冲正后不会自动恢复原意图。
- 迁移`663f3b4aacad`本地升级且无声明漂移。35项后端测试通过，包括并发预留、
  部分成交/取消/晚到成交、卖出不释放现金、出金失效、完成后冲正与到期释放。
  Ruff、生成合同和TypeScript/Vite构建通过。
- Chromium串联“创建100股×10元+5元预算→预留1005元、账面余额不变→
  刷新→取消→可支配现金恢复→独立核对一致”，累计场景7.9秒、深浅四视口通过。
  部分成交关联已通过数据库集成测试，浏览器尚未覆盖该分支。
- 数量规则随上海实际运行日复核至2026-09-16，证据见交易规则文档。
  首批计划显式为自主账本意图，未校验实时行情/权限/市值仓位风险，不能称为
  系统可执行建议；费用为人工预算，到期为读取时派生状态、尚无后台到期审计。
  期初持仓、公司行为、完整原子更正和系统联合计划仍待实现，M2未完成。

### 批次独立身份（T14/T18 前置）

- 持仓批次独立主键，成交来源保留为可空唯一外键，卖出消耗直接关联批次。
  迁移沿用原ID及全部成本/数量，不制造成交或现金；后续非成交来源可独立接入。
- FIFO内核使用lot_id，冲正重建先恢复批次再恢复消耗；独立对账核对成交来源。
- 本地迁移、35项后端测试、Ruff与Alembic无漂移通过，既有API合同不变。
  此步仅数据边界调整，期初持仓录入及公司行动尚待后续实现。

### 期初持仓录入（T14/T18/T20 部分）

- 新增不可变期初批次事实与分页API/页面，记录取得日、基准时间、实际剩余
  总成本和唯一凭据。账户锁、版本、幂等回执、来源防重；有成交/计划后关闭期初。
  不扣现金、不伪造成交，卖出FIFO和冲正重建保留来源与精确成本。
- 独立对账加入期初事实、批次来源、版本及取得日期检查；修正混合锁定批次
  的核对逻辑，跳过当日锁定批次后仍能消费后方已结算批次。
- 迁移`8cd8d36e4cf7`升级，38项后端测试通过，Alembic无漂移；
  Ruff、OpenAPI/客户端生成、TypeScript/Vite构建通过。两项第三方弃用警告仍在。
- 真实Chromium新增“零现金→期初37股/375.01元→刷新→卖出12股→现金143元、
  剩余成本253.39元、实现盈亏21.38元→独立核对”，移动端截图检查通过。
  既有全串联场景及深浅四视口继续通过；两场景共11.1秒，仅合成账户且已清理。
- 尚无期初更正、转托管、公司行动或真实账户迁移；M2未完成，未部署或切流。

### 计划到期审计与关联成交验收（T19 增量）

- 独立账本维护Worker每秒扫描，到期计划在账户锁内释放剩余预留、追加EXPIRE
  审计并推进版本；并发Worker跳过已锁定账户，重复扫描不会重复写事件。
  到期读投影仍即时排除占用，不依赖Worker可用性；晚到成交保留事实而不复活预留。
- 计划轮询检测新账户版本后刷新余额版本，避免后台到期后表单继续使用旧版本。
- 39项后端测试、Ruff和TypeScript/Vite通过；合成时钟验证并发到期仅写一次、
  现金不变、晚到37股仍保留到期状态及独立核对一致。Worker本地已启动。
- Chromium增加关联计划部分成交：100股计划→记录37股→剩余预留630元→刷新
  →取消只释放剩余预留，现金9049.99元与持仓保留。两场景9.0秒通过，
  包含期初持仓以及既有深浅四视口/研究流程。尚未完成平台所有模块的全流程验收。

### 公司行动现金事实（T18 增量）

- 现金分红与实际红利补税独立类型，强制证券和永久凭据编号；不当作外部
  入出金、不推断税率、不改持仓成本或伪造成交。补税与出金一样受余额约束，
  冲突的计划预留按实际现金变化失效。
- 迁移`0a722e336341`修正Alembic约束名重复加前缀后升级成功；41项后端测试、
  Ruff、Alembic无漂移、生成合同及TypeScript/Vite构建通过。
- Chromium完成143元现金→分红12.34元→补税2.47元→刷新152.87元、
  剩余股份成本253.39元不变→独立对账；两个累计场景10.8秒通过。
  数据为合成凭据，实际派息资格/税率未自动验证，送转配股及公司行动更正仍待实现。

### 成交原子替换（T16/T18/T20 增量）

- 冲正预览/确认支持完整替换股数、价格及实际费用，保持原交割编号、证券、
  方向、时间及计划关系。原记录不可变，追加修订事实与新旧现金差额，
  在原发生顺序重算后续FIFO和盈亏；相关计划按更正股数重算剩余预留。
- 独立核对分别重算原始记录及修订值，未复用写入端更正计算。
  历史缺股、负现金、变更预览、跨账户访问均失败关闭；重复并发确认只记一次。
- 本地迁移0008成功且Alembic无漂移，43项累计后端测试通过，追加计划更正
  用例单独通过（共3项替换集成场景）；Ruff、合同生成及TypeScript/Vite通过。
- Chromium验证12股×12元卖出改为10股×13元，现金152.87→138.87、
  剩余27股/273.66元成本、盈亏27.65元，刷新后修订凭据可见、对账一致。
  两个累计场景11.0秒通过，仅本地合成账户。截图已检查。
- 限制：单笔一次更正；多次修订链、时间/方向更正、资金/期初更正与股份公司
  行动尚待实现。

### 长页键盘跳转入口修复

- 将跳转链接的屏幕外定位改为默认裁剪、聚焦显示，避免全页截图滚动拼接时
  显示负坐标的固定元素。保持Tab首焦点和Enter跳转能力。
- 浏览器验证默认隐藏→Tab显示→Enter跳转，累计两场景10.7秒通过；
  深浅四视口、移动端截图及TypeScript/Vite通过。

### 真实股份转入（T18/T20 增量）

- 新增独立转托管事实、批次来源与分页API/UI，保存原取得日、实际到账时间、
  剩余总成本和唯一凭据。已有成交后可转入，不扣现金、不伪造买入。
- 冲正把转入按原版本插入重算，未来转入不能覆盖历史缺股；独立对账验证
  全部事实时间顺序、三种批次来源和成本。转入后禁止追加期初。
- 迁移08e085a79bba本地升级、Alembic无漂移；46项后端测试、Ruff、
  生成合同及TypeScript/Vite通过。两个第三方弃用警告仍在。
- Chromium完成27股基础上转入13股/130.01元→刷新40股/403.67元成本，
  现金138.87元不变→独立核对一致；两个累计场景13.4秒通过，
  移动端截图及深浅四视口检查通过，仅合成账户。
- 当前只支持转入；转出、送转配股、非交易处置、资金/期初及多次更正链仍待
  实现，M2未完成。新模型、真实Agent、迁移与上线未完成。

### 新训练数据准备核查（T21 前置）

- 增加只读归档审计CLI，真实核查19个分片、2016-01-04至2026-09-11、
  10,114,448行（含跨片重叠），计算SHA-256并检测片内重复/非法OHLC/量额。
- 证券元数据5,901条，其中339条有退市日；各日线分片仅0～1只匹配退市证券，
  最新片3,051只均为沪深主板。历史身份、复权、分钟因果股票池及Agent证据
  缺口明确列入不可发布报告，没有把旧训练数组改名为新模型。
- 合成归档/只读SQLite集成验证通过，Ruff通过；无生产写入、未运行训练。
  结果与可复跑命令见`market-data-readiness.md`，机器报告在本地/tmp。

### 全量规范行情自身审计（T07/T08/T21 前置）

- v4 数据集已完成2016-01-01至2026-09-15全部2,601个开放日，包含5,812只
  历史证券、11,071,942条日线和11,259,492条复权因子；主板、创业板、科创板、
  北交所均在因果股票池中，退市证券未按当前目录剔除。
- 新增规范SQLite专用只读审计CLI，逐证券核对生命周期、开放日覆盖解释、
  OHLC/前收/量额十进制、复权、源代码有效期、可用时间、checkpoint和分钟聚合。
  11,259,562个应有证券开放日中，187,620个无日线日期全部有停牌或暂停上市
  解释，无未解释缺口；4个既有分钟分区均通过48时点和日线聚合复算。
- 完整库`integrity_check=ok`，数据库SHA-256和报告SHA-256已固化在
  `market-data-readiness.md`，报告无违规项。新增2项审计回归；市场数据相关
  54项测试通过。独立公开源扩展抽样仍在处理供应商冲突，因此T08尚未完成，
  当前结果也不代表分钟episode、标签、模型或收益已经验收。

### 扩展日线独立交叉核验（T08 前置）

- 公开源核验协议升级为v2，增加东方财富原始日K及成交额，要求每个样本至少
  一个独立来源完整确认，同时单列其他来源冲突。新增协议测试覆盖东方财富单位、
  非法响应、来源冲突和成交额不一致。
- 真实扩展到2016/2019/2021/2024/2026五个年份、四类股票17个高成交额样本：
  14个自动确认，3个保守失败。2016年`SH.600030`差异由上交所当时的收盘价
  VWAP规则解释；北交所2026样本的量额差异由同日4笔大宗交易逐笔闭合，
  不是竞价日线缺失。东方财富本机有3次协议断开，报告保留失败状态。
- 这次核验暴露了数据语义缺口：规范`daily`量额是竞价时段口径，部分公开日K
  包含大宗交易。大宗交易事实和口径字段尚未进入规范数据集，因此T08继续阻断，
  不开始标签训练。机器报告与具体差异见`market-data-readiness.md`。

### 大宗交易分账与规范数据集v5（T08/T21 前置）

- 数据合同升级为`market-dataset.v4`，日线量额明确为竞价口径，大宗交易按证券日
  独立聚合。Tushare原始万股/万元使用Decimal转为股/元，保留源行哈希和21:00
  保守可用时间；空交易日也有独立checkpoint。
- 同步按自然月批量请求，6,000行边界自动二分并保持单日恢复。B股、基金、债券、
  未知代码、北交所开市前新三板记录和代码复用期外记录分类审计；其他生命周期
  越界硬失败。真实运行发现并覆盖`302132.SZ`历史非股票代码复用边界。
- v5已封存全部2,601个开放日、152,629条证券日汇总和279,882笔A股大宗交易；
  连同11,071,942条日线、11,259,492条复权因子及4个分钟分区通过全库审计，
  无违规项。数据库与报告SHA-256记录于`market-data-readiness.md`。
- 后端125项测试与Ruff通过。该结果完成竞价/大宗交易事实分账，但候选episode
  分钟覆盖、标签、模型训练、全市场回测及联合Agent验证仍未完成，不据此发布。

### 全量候选episode分钟需求与归档迁移（T08/T21 前置）

- episode合同升级至v3。196,748个候选按执行日起5个开放日展开981,740条关联，
  去重为222,500个证券日：222,088个需分钟数据，412个因当日无规范日线明确标记
  `NOT_APPLICABLE`；末端4个决策日共400个episode因不足完整窗口延后。
- 分钟导入按源文件事务化，记录文件哈希、逐证券日接受/拒绝、聚合偏差和重试
  状态。每个可用证券日必须有48个标准端点，并通过OHLC、整数股、开收盘、日内
  范围、成交量100股及成交额5bps门禁；失败时不保留部分K线。
- 隔离归档首批248个交易日写入21,421个证券日；随后验证19个带逐文件SHA-256
  审计的2016-2026 Tushare分片，再写入56,041个证券日。当前合计完成77,462个
  证券日、3,718,176根K线。
- 仍有144,626个证券日待取；其中6,321个保留明确质量拒绝，原因包括开收盘、
  成交量、成交额或48时点不一致，其余约13.8万个未出现在旧归档。缺口集中在
  创业板、科创板和北交所，必须通过当前Tushare适配器补取，不能以旧Top1000
  股票池替代全类型覆盖。
- 正式episode SQLite完整性为`ok`，所有完成证券日均恰好48根且无空完成标记；
  后端新增Tushare分片身份与逐文件哈希回归；全量测试与Ruff通过。

### 当前Tushare分钟全量补取（T08/T21 前置）

- 新增按证券和最多120个规范开放日切窗的`fetch-minutes`阶段。单次最多请求
  5,760根5分钟K线，低于8,000行截断门禁；响应按需求证券日拆分，每日独立
  验证和提交，中断后从`PENDING`继续。
- 真实使用当前允许列表内的Tushare兼容端点跑完3,218个剩余窗口；原先约13.8万
  个从未出现于旧归档的缺口已全部尝试。四板和2016年历史分钟均验证可取，
  没有认证、限流或网络中断。
- 当前222,088个应取证券日中208,903个通过，共10,027,344根K线；13,185个
  保留质量拒绝。拒绝包括开收盘6,462、成交量3,306、会话缺失1,630、成交额
  1,340、源OHLC非法431、越出日线范围16；不存在未尝试或无原因的`PENDING`。
- 196,748个候选中174,891个已有完整五日路径，分板块为主板56,073、创业板
  61,340、科创板39,980、北交所17,498；21,857个候选仍被至少一个质量缺口
  阻断。上交所2018-08-20前历史收盘制度差异和北交所2022～2023供应商口径
  是不同问题，未用统一放宽阈值掩盖。
- SQLite`integrity_check=ok`，完成证券日总数乘48与分钟行数完全一致。新增
  封存门禁：存在任何`PENDING`时拒绝生成manifest，因此当前数据集未封存，
  T08/T21、标签、训练和回测仍未标记完成。

### 分钟质量规则分层与二次补取（T08/T21 前置）

- 根据上交所官方规则，股票收盘集合竞价自2018-08-20实施；此前官方日收盘价
  是最后一笔交易前一分钟成交量加权均价，不能要求其必然等于5分钟K线末笔价。
  摄取器现仅对`SH`且早于该日期的收盘差异应用例外，开盘、高低范围、量额和
  会话完整性仍全部校验；审计明确记录分钟末笔价、官方日收盘价，并规定终端
  退出使用规范日线收盘价。来源：
  https://www.sse.com.cn/aboutus/mediacenter/hotandd/c/c_20180806_4607055.shtml
- 成交量聚合由单一100股阈值改为严格小于
  `max(100股, 规范日成交量×0.05%)`，与成交额5bps门限对齐。该规则是在
  5,744个沪市历史拒绝上审计后冻结：5,737个为分钟合计少于日线，4,484个
  低于5bps，最大长尾超过1.2%，长尾未被放行。所有拒绝现在保存日线与分钟
  OHLC、量额、差值和实际容差；相同响应重试会幂等刷新审计详情。
- 补取器支持按拒绝原因恢复，并按交易日选择`instrument_aliases`中的有效
  源代码、在代码迁移边界拆窗。北交所旧`43/83/87`代码真实重试305个窗口，
  仅1个证券日通过，3,068个变为会话缺失；当前授权端点无法提供这些旧代码
  对应日期的完整分钟序列，不能用当前`920`代码下不一致的数据替代。
- 二次补取后，222,088个应取证券日中213,908个通过，共10,267,584根K线；
  8,180个仍为`PENDING`：会话缺失4,698、成交量1,621、开收盘920、
  成交额494、源OHLC非法431、越出日线范围16。完整五日路径177,711 /
  196,748，分板块为主板58,047、创业板61,384、科创板40,082、北交所
  18,198。SQLite `integrity_check=ok`；因外部分钟数据缺口仍未清零，
  数据集保持未封存，训练与全量回测不得宣称完成。

### 豆包搜索与Agent受控终止协议（T09/T10/T11 增量）

- Agent总墙钟预算为180秒，最多4次豆包搜索；任务租约210秒。搜索使用固定HTTPS
  端点和仓库外凭据，保存请求ID、时延、来源权威等级、发布时间、证据ID和工具轨迹，
  结果标记`SEARCH_DISCOVERED/SEARCH_RESULT_UNVERIFIED`，不等同于事实已核验。
- 最终研判必须调用`submit_assessment`，参数使用`AssessmentOutput` schema；自由文本
  即使看似合法JSON也拒绝。搜索与研判只能引用任务时点前的包内证据，外部文本仍按
  不可信输入处理。
- 真实只读探针中，极小终止工具15.6秒完成，完整研判工具8.1秒完成，生产函数的
  无搜索合成研判21.2秒完成并通过合同；带真实搜索的生产路径39.9秒完成，3次搜索
  共约2.1秒，接受5条证据并生成`UNCERTAIN`研判。探针没有数据库写入。
- `tests/test_research.py`4项及相关Ruff检查通过，覆盖强制终止工具、自由文本拒绝、
  搜索证据因果时点、工具预算、审计轨迹和fenced发布。历史长尾超时仍需重复运行
  评测；一次真实成功不代表供应商SLA或研判质量已经验收。

### 来源耗尽、数量条件标签与全市场排序（T21/T22/T23 增量）

- 原候选episode库已将213,908个证券日标记完成、412个不适用、8,180个在全部
  已知来源尝试后标记`SOURCE_EXHAUSTED`，无未决需求；封存后176,914 /
  196,748个episode可用。多规模标签包含一手、10万元参考及20日中位成交额
  0.05%/0.2%/1%场景，共877,518条，10万元不再是训练过滤条件。
- 全市场排序库覆盖2016-01-01至2026-09-15、5,812只历史证券和10,223,999条
  点时样本；主板6,953,408、创业板2,354,483、科创板683,118、北交所232,990。
  数据库SHA-256为`b2338d8dcdeeede73587ee7068a3a5d095daabacbaa4dfe0d64ca0d2f39a759d`。
- 排序模型确认集为2024-08-13至2026-09-08，共2,648,931样本/503日；
  平均日RankIC 0.0991，Top10五日毛收益均值0.9844%，10bps粗压力后0.8844%，
  四板均有入选。该指标未使用分钟成交、账户资金或Agent，不是可发布收益。
- corrected enriched v2统一消费排序模型同源31维全市场特征，删除语义不一致的
  旧流动性Top25名次。相对初始基线，确认集`pFill` AUC 0.5455→0.5741、
  `pFullFill` 0.7583→0.7757、`pWinGivenFill` 0.5303→0.5420、
  `stopHazard` 0.5776→0.5987，Q10-Q90覆盖率71.35%→73.85%；收益MAE未改善。

### 全市场选择的分钟执行回测（T23/T37 前置）

- 首次覆盖审计证明旧流动性Top25标签只能覆盖全市场模型5,030个时间外Top10
  选择中的100个（1.99%），因此未将旧标签冒充全市场执行回测。
- 为全部5,030个选择建立独立episode库，18,452个唯一证券日经两个允许的
  Tushare兼容端点尝试；17,776个通过严格质量门禁，676个有至少两次尝试后
  标记`SOURCE_EXHAUSTED`。最终4,583个episode可用、447个不可用，覆盖率91.11%；
  四板可用数为主板2,666、创业板1,281、科创板554、北交所82。
- 新标签库含22,840个数量场景、20,324个至少成交一手场景。episode数据库
  SHA-256为`9747abd668aaf931ce0c863437e003cd18fd1518b9a8961ca34a32abfc261e15`，
  标签数据库SHA-256为`b5138c79fc1a919c7cab8fe74e8c5a23746b83d5dca68de2432d8c53f5f88961`。
- 排名Top10的4,583个可用路径在10bps附加压力后平均为-0.0886%。预先冻结的
  模型门禁`pFill × (expectedNetReturnGivenFill - 10bps) > 0`保留1,073条，
  其中925条成交，压力后平均+0.2739%，预测与实际相关性0.159。
- 结果不稳定：科创板压力后均值-0.7424%，2025年-0.0533%，预测效用五分位
  非单调；另有8.89%选择因可信分钟数据缺失不可评估。报告保持`UNAVAILABLE`，
  阻断项包括覆盖不足、板块/时期失败、单调性失败、账户资金回放、Agent包和
  联合消融。该报告是重叠的独立episode诊断，不是账户净值或已验证收益。

### 多账户规模资金回放（T29/T37 增量）

- 使用同一模型门禁按10万、50万、100万、500万元初始现金回放，最多10个并发
  持仓；每个槽位使用初始现金10%，高价股按一手保留，只有真实现金不足才跳过。
  计划先预留目标成本，账本只扣实际部分成交；同股重叠跳过，买入在同日退出
  之前处理，持仓按规范日线收盘价盯市，停牌沿用最近价格。
- 额外10bps压力后，四种资金规模总收益分别为+3.73%、+2.02%、+0.42%和
  -7.86%；压力最大回撤分别为10.85%、10.52%、10.69%和12.92%。500万元
  场景暴露明显容量外推失败，不能把小账户正收益线性放大。
- 四个资金场景中科创板均亏损，2025年均亏损；500万元下创业板也转负。
  账户报告SHA-256为
  `219dc83c98d3f3b18ea5652bd561cf1c9e162340870ca3564b0ad573aac8dd3e`，
  发布继续被账户规模、板块、时期、覆盖和联合消融阻断。

### 持仓配对动作模型（T26/T27 增量）

- `position-action-label.v1`在同一持仓状态和未来路径下比较HOLD、ADD、REDUCE、
  EXIT；动作从入场后下一交易日前30分钟开始，按每bar 5%参与率、100股离散数量、
  历史费用和5bps滑点执行，未将EXIT定义为买入收益取负。
- 封存630,311个多数量持仓状态，覆盖150,515个episode和四板；无约束最佳动作
  为ADD 278,659、EXIT 337,924、HOLD 13,018、REDUCE 710。REDUCE虽低频，但
  因容量和分笔费用的非线性存在真实监督信号。数据库SHA-256为
  `0ba2a8280e4ec88761112ebaea6ac72f998caa4a8a1c6d4f55ae0051665212a3`。
- 三个回归头预测ADD/REDUCE/EXIT相对HOLD增量，由组合约束仲裁动作。确认集
  169,679个状态，平均regret 6.29%、P90 regret 18.41%；模型选择动作相对HOLD
  平均-0.0371%，主板、创业板、科创板均为负，仅北交所接近零。
  artifact SHA-256为
  `a79f5f5d9df927bab5b002d2a808a913d066639184dde2d9481e2872d46f007a`，
  因无正增益、板块失败、REDUCE支持低及缺Agent联合回放保持`UNAVAILABLE`。

### Agent联合候选包（T25/T40 前置）

- `agent-features.v1`只编码已校验研判的论点状态、策略适配、事实/推断/假设计数、
  反证、不确定性和证据数，不把自然语言临时映射成交易分数。Prompt、工具schema、
  Agent模型和`research-assessment.v1`协议均以SHA/版本绑定。
- `joint-candidate-v2`绑定排序模型、enriched执行模型、账户回放和持仓动作模型
  artifact；缺失项只剩前瞻Agent特征数据集与训练后的联合模型。历史Agent结果
  不使用今天的LLM记忆伪造，须从现在开始积累前瞻样本。
- 联合包默认加载返回`JOINT_BUNDLE_NOT_RELEASED`，研究模式只能得到
  `UNAVAILABLE/NONE`且`allowsNewRisk=false`。manifest SHA-256为
  `66908290d57a0ded94c4ab350509bf514ac12c93ad78b4089f8bd945c24b456d`；
  当前未完成联合模型训练、Agent质量评测、联合/去Agent/去量化消融，因此不能发布。

### 联合持仓运行时仲裁（T25/T26 增量）

- 新增`position-action-reference.v1`，要求量化侧同时给出四动作相对HOLD的
  期望增量、Q10/Q50/Q90、止损风险、样本支持、趋势、期限、费用与校准引用；
  HOLD必须保持当前数量并使用零增量基线。现有只输出均值的持仓模型不满足完整
  生产合同，不能通过缺省值伪装。
- 新增`position-assessment.v1`，持仓Agent只提交结构化论点、反证、不确定性及
  来源信号；主力资金和订单流明确保存供应商、推导口径、发布时间、首次获取时间、
  可用时间和校验状态，不将供应商推导指标写成已证实的“主力身份”事实。
- 确定性仲裁器按硬风险、联合发布门禁、模型/Agent组件绑定、DecisionContext、
  时点隔离、账户数量约束的顺序处理。硬止损不等待模型或Agent，按当前可卖数量
  输出REDUCE/EXIT；无可卖数量保留风险阻断。普通动作必须来自同一READY联合版本；
  Agent与量化冲突、任一输入缺失/过期或未来证据均返回`UNAVAILABLE/NONE`，
  不冒充HOLD。
- `JointBundle`现绑定持仓Agent协议并校验运行时release引用；READY manifest若仍有
  blocker、缺失artifact或协议不完整会被拒绝。当前`joint-candidate-v2`仍因前瞻
  Agent样本、联合训练及消融缺失保持`UNAVAILABLE`。
- 聚焦与全量验证：`uv run pytest -q`为204 passed；`uv run ruff check src tests`
  通过。尚未实现Position Agent任务持久化、前瞻样本采集和完整联合模型训练，
  因此本增量只完成可审计运行时合同与仲裁链路，不代表AC08或M3验收完成。

### 联合架构影子发布与每日评估（T25/T30/T40/T41/T46 增量）

- 用户明确要求直接发布Agent+量化架构并按日回测迭代。当前候选不满足实盘收益
  门禁，因此发布为正式`SHADOW`模式而非伪造`READY`：
  `joint-shadow-20260916-v1`已原子写入仓库外
  `~/.local/share/stock-platform/joint-releases/active-shadow.json`。发布时重新校验
  ranking、quant、position artifact及账户回放SHA；活动指针绑定不可变manifest，
  `allowsNewRisk=false`。
- SHADOW允许模拟账户运行并保存唯一动作，真实账户固定返回不可用且不能据此创建
  新增风险计划。持仓页面展示活动版本、影子状态、门禁数、量化趋势、Agent论点、
  唯一动作及阻断原因；任务和决策刷新后从服务端恢复。
- 影子量化运行时不读取含未来结果的训练行。它从封存市场库截至决策时点重新计算
  当日全市场横截面特征，使用发布包绑定的ranking和position artifact；2026-09-16
  探针实际使用2026-09-15行情，官方年度交易日历给出的第五交易日为2026-09-22。
  当前position artifact只提供动作均值，非HOLD动作的Q10/Q50/Q90、stopHazard和
  support明确标记缺失；READY模式会拒绝这些不完整预测。
- 有效`research-assessment.v1`可规范化为影子`position-assessment.v1`，保留原
  evidence、发布时间、首次获取和可用时间；主力资金、订单流和实时交易缺失被写入
  uncertainties，不以零值代替。完整影子评估会写入`prospective_samples`，等待
  5交易日结果成熟后用于联合训练和消融。
- 首次每日周期
  `daily-20260916T105434Z`已运行并保存于仓库外
  `~/.local/share/stock-platform/daily-joint-cycles/`。10万/50万/100万元压力收益
  分别为+3.73%/+2.02%/+0.42%，500万元为-7.86%；分钟覆盖91.11%，成熟前瞻Agent
  样本0，因此结论为`KEEP_CURRENT_RELEASE`。每日运行不等于每日强制升级，只有
  所有冻结门禁通过才生成晋级候选。
- 恢复缺失的前瞻样本迁移revision `e4a9c6f87d10`和ORM；既有数据库无业务行被
  重写，新建临时数据库从base升级到head共25张表通过，随后决策发布迁移
  `0010_decision_publication`上线。当前全量验证为211 passed，Ruff、Alembic
  drift、TypeScript与生产构建通过。尚未完成Position Agent独立前瞻运行、
  样本成熟结算、四组联合消融及实盘READY晋级。

### 活动影子包真实运行时验收（T25/T30 增量）

- 直接使用活动指针`joint-shadow-20260916-v1`、封存市场库和真实
  `position-action-model-v2`运行本地PostgreSQL完整链路；未向请求注入量化值。
  合成模拟账户上的有效研究研判生成`READY/REDUCE`，并持久化
  `DecisionContext`、唯一当前`Decision`和`PENDING`前瞻样本。样本绑定
  `a-share-20160101-20260915-v5:20260915`市场快照，探针结束后已验证删除全部
  合成账户、证据、任务、决策和样本。
- 性能剖析确认热请求约5至7秒耗在每次扫描4.5GB封存SQLite查询最近交易日，
  模型推理约27毫秒。最近交易日按封存库路径和候选日期缓存后，同进程同交易日
  复用从约5.6秒降至约66毫秒；首次进程仍需约26至45秒完成工件hash校验和
  全市场特征截面构建。首次慢启动必须由部署预热处理，不能在API请求中隐瞒。
- 持仓决策聚焦测试更新为15项并通过，Ruff通过。本验收证明影子运行工程链路
  可用，不改变`allowsNewRisk=false`，也不代表联合收益门禁或实盘READY通过。

### 决策到执行计划关联（T34 增量）

- `READY`且仍为账户当前指针的交易动作可由用户显式确认生成执行计划；
  `ADD`映射买入，`REDUCE/EXIT`映射卖出，数量、价格边界、费用预算和有效期均
  取自已发布决策。`HOLD`、过期、旧指针、账户版本变化或执行合同缺失会拒绝。
- `execution_plans`新增唯一`decision_id`与`source`，系统计划固定标识
  `SYSTEM_DECISION/JOINT_DECISION_PLAN`。同一决策重复提交只返回原计划；
  计划确认只创建预留，不修改现金或持仓，后续实际成交仍由人工录入并沿用既有
  部分成交、取消和冲正规则。
- 持仓建议表仅对可执行动作显示“生成执行计划”，计划表展示联合决策来源。
  Alembic升级至`0011_decision_plan_link`且无schema漂移；相关后端21项测试、
  TypeScript检查和生产构建通过。

### 服务端复核监控与应用收件箱（T32/T33 增量）

- 每个已发布持仓决策建立`decision_monitors`记录并绑定当前决策、账户、证券和
  `reviewAfter`。监控默认暂停，用户从持仓建议显式启用后才由服务端Worker触发，
  避免默认消耗Agent预算；关闭浏览器不影响运行。
- 到期监控以决策和复核时点生成确定性`triggerKey`，状态先进入`TRIGGERED`，
  Worker重启时复用同一键，避免重复episode；评估失败进入`FAILED`，新决策发布
  后在保留用户启停偏好的同时更新下一复核时点。
- Outbox由Worker投递到独立`notifications`事实表，`source_outbox_id`唯一约束
  保证重放不重复。新增收件箱页面、未读计数和标记已读；联合决策、监控触发/
  失败、研究更新和计划变更共享同一通知来源。
- Alembic升级至`0012_monitoring_notifications`且无schema漂移；相关后端15项
  测试、Ruff、TypeScript检查和生产构建通过。当前未配置外部系统推送渠道，
  收件箱是已验收通道。

### 独立Position Agent前瞻运行（T26 增量）

- 持仓评估任务冻结当前股数、可卖股数、账户类型、触发原因和截至`asOf`可用的
  最多16条证据。没有仍在有效期内的专用产物时，Worker使用
  `position-source-assessment.v1`提示调用指定Agent；提示明确禁止输出买卖动作、
  数量、目标价格或修改账户风险，最终动作仍由组合引擎仲裁。
- Agent调用沿用任务fencing、210秒租约、工具白名单、因果搜索和不确定结果不重试
  规则。成功产物及新发现证据先持久化，再进入`position-assessment.v1`规范化、
  量化动作价值比较和前瞻样本；账户在调用期间变化会阻止决策发布。
- 专用持仓产物可在有效期内复用。Agent未配置时，SHADOW模式仍可显式使用旧
  `research-assessment.v1`，并保留规范化回退与实时资金流缺失说明；该回退不满足
  实盘READY门禁。Agent已启用但调用失败时返回不可用，不悄悄降级。
- Research/Position Agent及持仓决策聚焦测试共23项、Ruff通过；真实供应商前瞻
  运行仍需在部署验收中按预算执行，单元测试只使用合成响应。

### 全市场量化与Agent事件双路候选（T24/T31 增量）

- 新增持久`CANDIDATE_SCAN`任务。活动联合包对封存市场库在扫描`asOf`前的
  最近交易日重算全市场横截面并运行ranking模型；仍在有效期内、证据均在
  `asOf`前可用且策略适配包含`EVENT`的Agent研判独立召回，不把自然语言映射为
  量化分数。
- 合并结果逐项保留`MODEL/AGENT_EVENT`来源、模型rank、预期毛收益、Agent
  assessment/evidence引用、发布版本和市场快照。任务及结果存入PostgreSQL
  jobs，幂等指纹不包含服务端生成时间，网络重试返回首次冻结的扫描。
- 真实工件探针使用`joint-shadow-20260916-v1`和2026-09-15封存截面，得到
  5,416只有效股票；量化Top10与1只合成因果Agent事件标的合并为11只候选，
  Agent标的位于量化Top10之外且仅标记`AGENT_EVENT`。合成研判及账户外数据已
  清理。
- 市场页已接入异步联合扫描、任务状态和候选表。聚焦后端9项测试、Ruff、
  TypeScript检查和生产构建通过；SHADOW候选仅用于研究，不构成实盘建仓许可。

### 策略实验注册、联合包v2发布与回滚（T36～T42）

- PostgreSQL新增不可变`strategy_versions`、`experiments`和`release_records`。
  策略按配置hash生成新版本，显式`DRAFT → FROZEN → EVALUATED`；同一冻结版本
  只能消费一次确认集。实验失败、样本覆盖、排除原因和全部四组指标均保留，
  不只展示成功结果。迁移`0013_experiment_registry`与`0014_release_registry`
  已升级，Alembic声明无漂移。
- 四组消融固定为完整联合、去Agent、去量化及持有公式基线；使用同一成熟样本、
  动作反事实、费用、风险和模拟政策计算，并输出平均/中位费后收益、正收益率、
  相对基线和95%区间。当前前瞻成熟样本为0，实验
  `720eb394566a478987a41177bebc93b6`诚实记录为
  `FAILED/MATURED_SAMPLE_SUPPORT_INSUFFICIENT`，四组收益均为空，不声称联合增益。
- `joint-bundle.v2`同时绑定ranking、quant、position模型，账户回放，冻结策略，
  消融报告，通用研究Prompt、专用Position Prompt、工具schema及Agent协议。
  包内策略和消融工件逐文件SHA-256校验；路径穿越、组件错配、Prompt缺失、
  指针releaseId与manifest不一致均拒绝加载。
- 候选`joint-candidate-20260916-v3`已通过完整性预加载，并按用户指示原子切换为
  活动`joint-shadow-20260916T123545Z-b67df08a`。指针manifest SHA回读一致，
  数据库只有一个ACTIVE发布记录；状态为`SHADOW`、`allowsNewRisk=false`，
  15项收益、覆盖、Agent质量和样本门禁原样保留。
- 文件指针切换使用进程间锁、期望活动版本和`os.replace`；冲突不覆盖现有指针。
  回滚只接受已完整预加载的v2历史包，并创建新的回滚审计记录，不改账户账本。
  当前只有一个真实v2活动包，因此尚无真实v2历史目标可执行回滚；双v2切换与
  回滚已由临时不可变包测试覆盖，旧v1包不冒充v2回滚目标。
- 发布权限固定为当前活动发布记录所有者；首次发布时为最早平台用户。其他登录
  用户可只读查看全局发布历史，不能登记候选、激活或回滚，Agent没有发布入口。
- 策略实验室已替换占位页，提供策略版本详情、冻结/运行操作、四组指标、失败代码、
  发布门禁、候选登记、激活历史及回滚入口。真实Chromium在1440px和390px下读取
  当前策略、0/2,000样本消融及15项门禁，无横向页面溢出、控制台错误或失败请求；
  截图为`test-results/strategies-experiment-desktop.png`及
  `test-results/strategies-release-mobile.png`。
- 全量验证为222 passed；Ruff、OpenAPI生成、TypeScript/Vite生产构建和Alembic
  drift检查通过。T36～T42的工程能力已完成；AC14收益证据与实盘READY仍被0成熟
  样本和既有模型门禁阻断，后续由M5每日成熟结算和受限迭代继续积累。

### 全市场排序目标与LambdaRank重训（T23研究增量）

- 保持封存排序库`full-universe-ranking-v1`只读，直接从其中10,223,999条原始
  五日收益按交易日派生`global-percentile-v2`标签；未复制6.8GB数据库，也未覆盖
  v1 artifact。训练CLI显式选择标签策略和模型族，默认仍保持旧行为。
- 全市场标签加原HGB/MSE的候选
  `full-universe-ranking-model-global-hgb-v2`未产生提升：确认集平均日RankIC
  `0.09907`，Top10五日毛收益`0.8704%`，低于v1的`0.9844%`；该候选判定失败。
- LightGBM 4.7.0 LambdaRank以交易日为query、五级整数relevance训练，在校准集
  NDCG@10/50早停于第73轮。候选
  `full-universe-ranking-model-lambdarank-v2`的确认集Top10五日毛收益为
  `1.5592%`，10bps粗压力后`1.4592%`，但全横截面RankIC为`-0.01379`。
  artifact SHA-256为
  `b4a4bfcbada254346bc29d17dc771931f79535078c9f6732ed7ad56cb8a4cdb7`。
- LambdaRank按年份的Top10毛收益为2024年`1.8285%`、2025年`1.9584%`、
  2026年`0.8224%`。相对v1的按日配对增量为`+0.5748%`，10交易日块bootstrap
  95%区间为`[-0.0606%, +1.2322%]`，仍跨0，不能判定稳定胜出。
- 现有分钟标签只覆盖LambdaRank新选择的`97 / 5,030`，远低于可用于执行回测的
  范围。该候选保持`UNAVAILABLE`，未改活动SHADOW指针、联合包、部署任务或
  `allowsNewRisk`；下一步必须为新选择补齐独立分钟episode并完成费后回放。

### LambdaRank分钟回放与严格walk-forward（T23研究增量）

- 新建并封存`full-universe-lambdarank-episodes-v2`：503个交易日、5,030个
  Top10候选、12,573个唯一分钟证券日。3,687个证券日从两个已封存episode库
  读取后重新执行当前日线一致性校验，其余通过Tushare接口补齐；最终11,995个
  证券日通过，578个在两次独立抓取中稳定复现分钟完整性或日线量价校验失败，
  逐项记录`SOURCE_EXHAUSTED`，不存在未尝试缺口。episode数据库SHA-256为
  `d239314c72319607a0ecbbbd5bfbc90fdfc4a3c950bd4b5c39099a4c998c5db6`。
- 标签库`full-universe-lambdarank-labels-v2`覆盖4,500/5,030个episode，
  覆盖率`89.46%`，含22,339个规模情景；530个不完整路径明确排除而非按零收益
  填充。覆盖报告SHA-256为
  `ca3a21fe12252de17c59d68c3fbef13c92be35726cee1196e048ce06b9cb8dbe`。
- 费后执行回放使用既有`quant-short-horizon-enriched-v2`，模型门禁放行
  504/4,500个可观测候选，额外10bps压力后平均收益`+0.3240%`。但北交所为
  `-0.4619%`、科创板为`-0.0678%`、2026年为`-0.8308%`，预测效用五分位也
  不单调；报告保持`UNAVAILABLE`，SHA-256为
  `15cde23196d505f0f152a9036c43c5d412a977b72ea877fa8716221883074526`。
- 新增固定协议`ranking-walk-forward.v1`：5个连续外推窗口、每窗100～101个
  交易日、126日滚动校准、训练/校准间5日purge、校准/测试间5日embargo；
  每折重新训练全市场LambdaRank候选和板块百分位HGB基线，超参数试验次数固定
  为1。503日聚合Top10毛收益为候选`1.1202%`、基线`0.8994%`，日均增量
  `+0.2209%`，但10日块bootstrap单侧95%下界为`-0.3995%`。
- 五折候选RankIC均值`-0.01117`、中位数`-0.01689`、最差`-0.04411`；
  2026年候选相对基线退化`-0.6994%`，MAIN板块日收益贡献退化`-0.5387%`。
  RankIC与相对收益置信下界两项预注册门禁均失败。walk-forward报告SHA-256为
  `18f259b9391d1773aecef6aa5bd445749703ced26418054e0d4407ba15e175d9`。
- 全过程未修改活动指针；仍为`joint-shadow-20260916T123545Z-b67df08a`，
  `SHADOW`且`allowsNewRisk=false`。本轮结论仅为离线候选失败证据，不部署。

### 成熟结算、复盘提案与每日学习（T43～T47）

- 迁移`0015`～`0017`新增实际执行结果、归因、ReviewReport、改进提案与周期漂移
  报告。成熟结算只读封存市场事实；缺日线、分钟窗口、复权因子或公司行动无法
  解释时标记`EXCLUDED`。模拟反事实与真实执行分账，用户未建计划/未录成交时
  `eligible=false/failure=null`，不算策略失败。
- Review Agent只能引用Python生成的metricId和episode ID；数字注入、越权引用
  和无来源提案拒绝。Strategy Agent只能从版本化白名单选择单一允许值，编译结果
  仅创建新`DRAFT`策略，不写活动发布。发布后的实验参数随联合包进入在线仲裁，
  不会只换版本名而不改变行为。
- 每日状态机按质量检查、结算、复盘、提案、冻结实验、比较、SHADOW发布逐阶段
  恢复。联合组必须优于去Agent和持有基线且95%下界为正；否则保留当前版本。
  漂移仅比较同horizon、模拟政策、量化包、Agent协议和特征schema；20交易日周期
  因未训练明确显示`UNSUPPORTED`，不把5日收益伪折算为中期收益。
- 当前数据库没有成熟前瞻样本，因此工程链路已实现但尚未产生一轮可信的“合格
  实验→新冠军”收益证据；活动版本仍为
  `joint-shadow-20260916T123545Z-b67df08a`。

### M1/M2缺口回查（T12/T18/T20）

- 迁移`0018`增加用户隔离的市场保存视图，只保存查询条件和关注模式，不复制候选
  数据；刷新可恢复，跨用户读取/删除被拒。
- 迁移`0019`增加券商已结算送股/拆股事实。新增股数按现有未平批次比例分配，
  总成本和现金不变；记录并发幂等、账户版本递增，并由独立账本重放验证。

### 本机部署、迁移与恢复验收（T48～T51/T54工程范围）

- 新增无密钥容器/Compose模板、GitHub CI、同源静态Web托管、CSP/权限安全头、
  维护只读开关、部署preflight、旧状态快照原子迁移工具及生产恢复手册。
  `0020_prospective_immutability`将本机曾手工存在的前瞻样本/结果不可变触发器纳入
  Alembic，干净数据库与当前数据库约束一致。
- `platform-ops backup/verify-restore`已实际创建仓库外PostgreSQL备份并恢复到临时
  数据库；备份SHA-256为
  `72fc6c42a4236931a9e9ade76d2ddd9a1467cbe57a3094701d2a128b59c029ba`，
  恢复revision为`0020_prospective_immutability`，关键事实表行数成功回读。
- 本机同源部署运行于`http://127.0.0.1:8080`，健康接口回读代码revision
  `025f370834e3-dirty`（共享工作区仍有用户并行模型改动）、数据库连接和活动SHADOW包；API、Research、Decision、
  Review、Portfolio Worker均启动且无运行错误。
- 全量后端`242 passed`，Ruff、Alembic drift、OpenAPI生成、TypeScript、Vite
  生产构建和pnpm高危依赖审计通过。部署端真实Chromium三条E2E通过，覆盖今日、
  策略、复盘、账户资金、T+1、冲正、导入、部分成交、送转、保存视图、四视口、
  深浅主题、刷新恢复和退出隔离，无控制台错误或页面横向溢出。
- 当前会话加载了TRAE-browseruse技能，但工具层未暴露任何`browser_*`调用；已用
  Playwright真实Chromium完成同等回退验收。Docker二进制在本机不可用，容器模板
  未本机实跑；本机进程部署已实跑。
- 未提供真实旧账户快照和旧系统最终写入水位，因此真实迁移、生产真实账户只读
  核对和写权限切换未执行。迁移工具只用合成当前状态快照验证账户级事务与幂等，
  不声称生产迁移完成。

### 阿里云与Vercel只读SHADOW部署（T48/T51云端增量）

- 新建RDS PostgreSQL 17 Serverless实例`pgm-bp104rk1173o79ec`，按量计费、
  20GB、运行状态`Running`且开启删除保护；数据库和普通DBOwner账户已建立，
  密码只保存在仓库外系统钥匙串。FC与RDS位于同一VPC，未公开数据库地址。
- 新建FC 3.0函数`stock-platform-v1`，Python 3.12自定义运行时、1 CPU/2GB，
  使用VPC访问RDS。预留实例目标和当前值均为1，`alwaysAllocateCPU=true`，
  按量实例已关闭，避免Worker重复写入。
- 生产健康接口通过Vercel回读`status=ok`、`database=connected`、
  revision `ebc8f6f7939b-dirty`和活动包
  `joint-shadow-20260916T123545Z-b67df08a`。运行时保持`SHADOW`、
  `writeEnabled=false`和`allowsNewRisk=false`；Agent、搜索与市场外部调用仍关闭，
  等待云端密钥服务注入后另行验证。
- Vercel生产部署状态为`Ready`，入口为
  `https://stock-dashboard-one-plum.vercel.app`。修正FC生产Origin后，真实
  Chromium在1440x900和390x844下均加载200、无页面溢出或脚本异常；匿名会话和
  错误登录均返回预期401，证明GET/POST反向代理链路可用。浏览器控制台只记录
  这两个预期401资源响应。
- 旧FC `stock-dashboard`的21个timer触发器已全部设置为`enable=false`；旧HTTP
  触发器、函数代码和数据保留为回滚入口，未删除。旧
  `stock-dashboard.vercel.app`不属于当前Vercel账户上下文，未强制重绑。
- 2026-09-16已从旧OSS revision `7076`的只读最终快照迁移“飞飞徐”当前状态。
  迁移输入SHA-256为
  `24e9a9180d07d3506cfbc1138647c19ef91516b2e60ebea6b28cb5d0f5588238`；
  首次返回`MIGRATED`，重复调用返回`ALREADY_MIGRATED`。迁移后现金
  `19418.00`元、三只持仓合计`1100`股、剩余含费成本`61590.66`元，独立账本
  对账通过。238条历史平仓和旧计划等未作为新成交伪造，历史收益仍标记不可重建。
- 迁移通过同VPC、无HTTP触发器的一次性FC执行；完成后函数、私有OSS代码包和
  本地密码哈希均已删除。RDS新增独立`fc_vswitch`白名单组，仅允许FC所在
  `10.88.1.0/24`网段，仍无公网数据库地址。
- 新账号密码规则统一为严格5位ASCII数字，CLI与登录页同步限制。真实Chromium
  已验证四位输入被浏览器拒绝、旧五位密码可登录、迁移现金和三只持仓可见、
  页面控制台干净且退出成功。修复登录成功时清空活动session查询导致匿名重取的
  缓存竞态；Vercel生产别名已更新。
- FC健康接口回读revision `ebc8f6f7939b-dirty-password5`、
  `database=connected`且`writeEnabled=false`。相关身份和迁移测试`8 passed`，
  Ruff与Web生产构建通过。全量后端为`243 passed, 8 failed`；8项失败均因
  已核验交易规则截止`2026-09-16`而测试运行时上海日期已进入`2026-09-17`，
  与本次密码和迁移改动无关，未把该日期证据缺口误记为通过。

### 模型组合训练启动（2026-09-17）

- 用户授权按“全市场目标→时间外融合→分钟执行与校准→持仓与 Agent 联合”
  实施，缺数据通过 Tushare 补齐，验证合格后替换线上方案。
- 任务及冻结原则记录于
  [模型组合实施记录](model-combination-implementation-2026-09-17.md)。
  全量训练已启动并完成首折四模型/融合，后续折运行中；未变更线上包。
- 发现此前20级实验使用默认指数gain，不能用于本轮统一线性gain的等级比较。
  封存联合消融仍为0有效样本，需查询最新状态并推进前瞻采集。
- 基线检查：`uv run pytest tests/test_ranking_model_trainer.py
  tests/test_ranking_walk_forward.py -q`，`7 passed in 4.83s`。
  仅验证已有训练与切分代码，不代表新候选收益验收。
- 增量：新增独立可恢复组合实验入口，冻结数据/源码哈希；保存逐折模型、时间外
  预测、收缩权重、留一模型消融与候选并集。首折101日产生4097个候选episode，
  Tushare补数运行中，产物位于仓库外`combination-20260917-v1`。
- 修复已实测的历史北交所分钟代码空结果：仅有明确证券别名映射时重试当前代码，
  仍要求完整48根及日线对账。`430090.BJ → 920090.BJ`的20241105真实请求通过；
  别名及分钟相关20项测试通过，Ruff通过。
- 已查询本地样本及线上只读状态；生产Agent/搜索/市场外部调用均关闭。四板块
  本地当下研究探针已提交，但出现超时和逐字引用拒收，尚不能构成有效联合样本。
  具体训练、测试与未完成项见实施记录；当前不可发布。

### 原系统恢复本地验证（2026-09-17）

- 恢复后的 Node/Vite/FC/OSS 工作台执行 `npm run test:ci`，结果为
  `2438 passed, 0 failed`；`npm run build`、`npm run package:fc` 和
  `git diff --check` 通过。
- 修复高波动突破候选的条件入场合同：风险、止损与预算引用所选路径的入场价；
  受信任路径的同价观察位保留路径有效期，未放宽普通观察价的证据或距离校验。
- `s.yaml` 中 20 个 FC 定时器均设为 `enable: false`。本地恢复构建可继续打包，
  且 `npm run package:fc` 已成功生成 FC 运行包。
- 设备授权入口及生产配置已删除；`/api/site_access` 返回 `404`。账户登录仍是唯一
  用户身份入口，生产密码、令牌和会话未写入仓库、文档或浏览器输出。
- 同一构建已部署到 FC `www.tedixtf.cn` 与 Vercel
  `stock-dashboard-one-plum.vercel.app`，两端均加载
  `assets/index-gsrYDoB8.css`；FC `/__health` 返回 `ok`。Vercel 固定使用
  `npm ci`，避免旧 pnpm lock 影响部署。
- 已在生产移动视口逐项点击今日作战、持仓与资金、个股详情、盘面研究、交易复盘、
  预警规则与命中记录、研究助手、策略日报、主题和账户设置入口；刷新后无控制台错误。
  自主加仓、做 T、卖出表单均成功打开并展示价格、整手、费用及 T+1 可卖约束，
  验收未提交任何成交、计划或设置写入。
- 浏览器发现并修复移动端持仓“更多操作”菜单被下一吸顶面板拦截的问题；展开期间
  仅提升持仓区层级并解除卡片裁剪，UI 合同测试和生产点击复测通过。
- 单股更新在当前活动模型包下明确返回
  `TRAINED_MODEL_NOT_READY`；生产 `/model_info` 显示模型版本
  `opportunity-score.20260910T200644Z.ensemble3` 已加载，但
  `productionEligible=false`。批量更新可保存 Agent 结果，过期收盘决策会被
  有效期门禁隐藏。因此失败关闭已验收，联合建议正常展示尚不能标记通过，需先让
  模型通过生产门禁后再验收。

### 模型组合五折与执行/持仓试跑（2026-09-17）

- 全市场5/20/100级LambdaRank与连续回归、等权/时间融合全部五折完成。
  等权Top10五日毛收益均值1.8216%，rank5基线1.3595%；等权末折-0.2307%。
  留一消融未证明每个专家都有稳定增量，不能将整体均值当作费后或独立确认成绩。
- 已生成22,250个OOF候选episode并启动全五折分钟补齐；检查点31,981分钟日通过、
  36,029待处理，持续日志位于仓库外`combination-20260917-v1/minutes-all-folds.log`。
- 新执行训练支持部分成交比例及滚动概率/区间校准，旧封存样本五折试跑完成；
  区间覆盖率仍有73.49%—87.39%波动。新持仓目标价值函数移除下一日真实容量特征，
  旧样本末折相对HOLD为-0.7409%，尚不满足发布条件。
- 先采集证据再单次Agent的四板块前瞻探针3个通过、1个因引文不匹配拒收。
  它们是研究捕获，尚未进入联合前瞻样本表或形成成熟标签；没有伪造历史Agent。
- 23项相关测试与Ruff通过。具体结果和阻断见
  [模型组合实施记录](model-combination-implementation-2026-09-17.md)。
  本轮未替换线上模型，持仓/费用/联合独立确认仍待完成。

### 模型组合前瞻配对与成熟结算（2026-09-17）

- 首轮分钟补数已结束，当前重试检查点65,003/68,010通过，3,007待处理；
  缺失集中北交所。费后对照、执行与持仓并集训练已接入顺序任务，结果尚待生成。
- 新封存最新日线/复权快照，基于5,417只股票冻结四模型与融合预测。
  四板块Agent配对试点3个PENDING、1个引文失败EXCLUDED；成熟结算实测0个MATURED。
- 已冻结48只模型候选并集并启动Agent采集。观察期20260917—20260923，
  输入哈希、固定成员、过期校验与未来结果隔离已实现；不等同已拟合联合策略。
- 22项相关测试和Ruff通过，提交`9fb6d52`、`c30790e`、`dae7d55`。
  联合前瞻增量、账户回放与独立确认仍不具备，未替换线上方案。
  详情见[模型组合实施记录](model-combination-implementation-2026-09-17.md)。

### 当前模型与 Agent 生产直用验收（2026-09-17）

- 按用户授权允许未通过晋级门禁的当前模型用于人工决策参考，但没有伪造门禁状态。
  生产仍回读`opportunity-score.20260910T200644Z.ensemble3`、
  `productionEligible=false`和`usagePolicy=DIRECT`；模型与 Agent 只生成建议，
  不自动下单或写入成交。
- 前端决策依据在模型版本旁显示“实验直用，未通过生产晋级”，模型训练发布页显示
  “当前直用版本”和人工确认提示。真实卡片已核验 Agent 研判、最强反方、复核逻辑、
  失效条件及证据局限均可展开查看；休市后的收盘建议因有效期不足继续失败关闭。
- 首轮批量验证暴露 Agent 端点容量问题：4路及2路并发均出现45秒超时。主决策默认
  并发降为1后，单只仍在45秒边界失败，因此将 Agent 有界超时提高到90秒、普通任务
  预算提高到100秒并保留零重试。重新发布后生产批量更新`12/12`全部成功，页面明确
  显示`1/1路生成中`。
- FC `stock-dashboard`和Vercel生产均已发布；主域加载
  `assets/index-4kd-DuAH.js`，Vercel部署为
  `stock-dashboard-fceodroz0-natmatthias172-6311s-projects.vercel.app`并保持
  `stock-dashboard-one-plum.vercel.app`别名。
- 生产移动视口只读验收通过：预警页可见23条监控规则、已触发状态和观察复核状态；
  加仓表单显示价格、整手和费用；做T表单显示底仓、成本、累计做T收益、买卖方向、
  价格、整手和费用；卖出表单显示今日可卖3手、价格、整手和费用。所有表单均未提交。
  交易复盘可回读真实成交、费用、做T收益、决策闭环和每日流水。
- 浏览器控制台无错误，网络记录无4xx/5xx失败。针对性42项测试、生产构建和FC打包
  通过；修正预算及单路契约的旧断言后，全量Node测试`2439 passed, 0 failed`，
  `git diff --check`通过。

### 模型组合正式门禁结果（2026-09-17）

- 68,010个分钟需求已封存：65,003个完成、3,007个数据源穷尽、0个未处理；
  22,250个候选episode中19,540个可用，生成97,001个执行情景标签。
- 等权组合在单手、10万元、成交额5/20/100bps情景的费后净收益分别为
  -0.6977%、+0.0037%、+0.0581%、+0.0615%、-0.0311%。比较仅覆盖88至99个
  共同完整日期，选择偏差显著，10万元和100bps情景置信下界未通过。
- 正式执行模型的pFill在第4、5折不优于发生率基线，净收益MAE在第1、2、4折
  不优于训练均值。正式持仓动作价值五折相对HOLD为+0.2455%、-0.3403%、
  +0.1631%、-1.4091%、+0.5944%，均未满足稳定性门禁。
- 504日长窗rank100虽比等权毛收益高0.2532个百分点，但单侧95%下界为
  -0.3023个百分点；252日窗口更差。CatBoost单模型毛收益0.8866%，相对等权
  差-0.9349个百分点，单侧95%下界-1.7557个百分点；加入五模型等权后也退化。
- 由于下游费后、执行、持仓门禁均失败，且48只Agent联合前瞻候选仍为
  32个PENDING、16个EXCLUDED、0个MATURED，本轮没有可替换模型，不继续安装
  TabM消耗资源。最早2026-09-23上海17:00后尝试结算前瞻样本。
- 活动联合指针保持`joint-shadow-20260916T123545Z-b67df08a`，生产
  `https://www.tedixtf.cn/__health`实测返回200/`ok`。现有ensemble3仍按上节
  记录作为实验参考直用；未把失败候选替换上线，也未修改账户事实或写权限。

### TabM深度候选训练结果（2026-09-17）

- 用户后续明确要求启动深度模型训练，因此上节“停止安装并盲训TabM”仅代表当时
  检查点，现已由新指令替代。依赖锁定为`tabm==0.0.3`、PyTorch 2.13系列，
  仅位于research依赖组；生产Node/FC运行时未引入深度训练依赖。
- 新增可恢复的离线`ranking_tabm_runner.py`，支持TabM、TabM-mini和普通MLP；
  保持冻结数据、五折时间隔离、基础预测哈希、时间外融合及`UNAVAILABLE`发布状态。
  训练预处理只拟合训练窗，TabM成员独立计算损失，模型和预测均带SHA-256收据。
- Apple M4 MPS前后向通过。10万行、fold 1、TabM-mini、1 epoch冒烟训练耗时
  3.59秒，全流程249.9秒，峰值RSS约1.53GB；缩减运行明确不可发布。
- 完整训练行的fold 1固定5轮TabM-mini预筛已完成：RankIC 0.07373、Top10
  毛收益2.0974%，低于`base_equal`的5.1979%；差-3.1005个百分点，10日块
  bootstrap单侧95%下界-6.3858个百分点。
- 提交`f17c3a7`增加训练窗内部63日验证、5日purge和patience=3早停，外层
  fusion/test不参与epoch选择。普通MLP最佳第1轮，RankIC 0.02762、Top10
  毛收益1.6642%，相对基线低3.5337个百分点；完整TabM最佳第2轮，RankIC
  0.03923、Top10毛收益2.0289%，相对基线低3.1690个百分点。两者单侧95%
  下界分别为-6.7035和-6.2950个百分点，时间外融合也均未改善基线。
- 聚焦测试`5 passed`，Ruff及`git diff --check`通过。三个深度候选均未达到
  五折三种子扩展条件，停止追加训练；报告保持`UNAVAILABLE`。活动指针仍为
  `joint-shadow-20260916T123545Z-b67df08a`，未修改线上模型或账户事实。

### Agent 生成过程与操作说明改版（2026-09-17）

- 个股“更新决策”区域增加读取行情、决策评估、Agent研判、核定保存四阶段。
  Agent区原位显示“现在怎么做、为什么这样做、什么情况先别做、到价后检查、
  什么时候作废”；服务端核定动作先显示，其余内容生成后逐项替换。
- 本地SSE和云端任务进度均支持只含可公开最终字段的`agentPreview`；不展示隐藏
  思维链，不允许Agent修改服务端动作、价格、手数或交易事实。
- 移除个股详情中的二次“白话解读”入口，只保留一套操作说明。新内容限制字数并
  禁止内部字段；历史内容也在展示层压缩、翻译内部概率/风险字段，信息缺口最多三项。
- 全量测试、针对性214/162/89/17项回归、生产构建、FC打包及
  `git diff --check`通过。生产472px移动视口确认生成区无横向溢出、四阶段与五项
  输出槽位可见、控制台无错误，最终资源为`assets/index-BkeagjZS.js`。
- Vercel CLI返回`Not authorized`，独立Vercel镜像未更新；阿里云FC主域
  `www.tedixtf.cn`已发布。生产触发时量化服务本轮返回
  `TRAINED_MODEL_NOT_READY`，因此只验收到Agent前的真实进度，没有伪造最终文本。
- 后续定位确认该错误有两层误判：证据不完整时编排器曾跳过量化调用；量化已返回
  `READY + DIRECT`但账户规则未选出可执行路径时，Agent又只读取
  `selectedDecisionPlan`。现已始终调用DIRECT模型，并从三条候选路径保留费后
  预期最优的`advisoryOpportunityScore`供Agent解释；执行动作、价格和手数仍由
  确定性账户规则核定，不自动下单。
- Agent优先读取已选执行路径，缺少可执行路径时回退上述READY直用分数，不再把
  “当前不允许执行买单”误报为`TRAINED_MODEL_NOT_READY`。模型状态仍真实保留
  `productionEligible=false`，没有伪造晋级。
- 新增两层回归覆盖并修正“VWAP/分时均价”展示契约；定向61项及全量2442项测试、
  Vite生产构建、FC打包和`git diff --check`通过。FC主域与Vercel生产镜像均已
  重新发布，主域健康检查通过，量化台账回读当前模型为DIRECT直用。
- 在线建议Harness需要仓库外`HARNESS_NICK/HARNESS_PASSWORD`，本次执行环境未
  配置，未把生产密码写入命令或自动化浏览器；因此账户级单股Agent完整生成仍需
  登录后手动点击确认。
- 修复量化服务直用状态的响应层级兼容：`/decision-score`顶层返回的
  `usagePolicy=DIRECT`、模型版本及发布状态会下沉到未重复携带这些字段的单条预测，
  不再把已加载模型错误归为`QUALIFIED`。
- Agent选取模型结果时会跳过路径上的不可用占位项，优先使用同轮
  `READY`模型结果；无真实模型响应时仍失败关闭，但用户提示改为“模型服务本轮未
  返回有效结果”，不再误报模型“未训练”。
- 修复已同步发布至阿里云FC主域和Vercel生产别名。生产量化端点实测
  `modelLoaded=true`、`state=READY`、`usagePolicy=DIRECT`，版本为
  `opportunity-score.20260910T200644Z.ensemble3`；主域健康检查、FC健康检查、
  主域行情接口和Vercel首页均返回HTTP 200。相关67项回归、生产构建及FC打包通过。
- 全量Node测试为2416/2417通过；唯一失败是既有选股UI源码文本测试仍在
  `StockPickTab.jsx`内查找已集中到`shared/stockPickModes.js`的模式文案，与本次
  模型状态适配无关，本次未混入该项修复。

### 闲置阿里云资源清理（2026-09-17）

- 杭州地域ECS实例、独立云盘和EIP清单均回读为0，无ECS关联计费资源需要停机
  或释放。
- RDS Serverless `stock-platform-v1`仅被已停用的Python重建FC引用；当前主域、
  Vercel和`stock-dashboard`均不使用该数据库。旧FC无域名、触发器、别名或版本，
  写入、市场与Agent能力均关闭。
- 已移除旧FC `stock-platform-v1`的LATEST预留实例，目标值和当前值由1清空；
  该函数继续保持`disableOndemand=true`，不再产生运行计算费或数据库连接。
- Serverless RDS不支持暂停，`StopDBInstance`返回`IncorrectDBInstanceType`。
  仓库外备份`platform-20260916T1432Z.dump`已复核SHA-256仍为
  `72fc6c42a4236931a9e9ade76d2ddd9a1467cbe57a3094701d2a128b59c029ba`，
  与已完成的恢复验证一致。随后关闭删除保护并释放只读迁移副本
  `pgm-bp104rk1173o79ec`。
- 清理后回读杭州地域ECS为0、RDS为0、旧FC预留配置为空；`www.tedixtf.cn`
  健康检查正常，主域和Vercel入口均返回HTTP 200。OSS交易事实、当前Node/FC
  工作台和量化服务未修改。

### 单股Agent超时修复（2026-09-17）

- 泰坦股份生产截图复现`联合决策不可用：Agent响应超时`。生产内置最小模型测试
  显示两路解释模型和日报模型约2.3～2.8秒返回，原助手
  `gpt-5.6-terra`最小请求超过130秒；直接运行
  `generateDecisionAgentGuidance()`的最小复现于90.1秒稳定超时。
- 原因是助手使用单一高尾延迟端点；页面同时存在自动复核任务时，上游队列会放大
  尾延迟。提示词与token上限不是主因：仅保留必要输入、256/400 token仍超时。
- 两路既有`claude-sonnet-5`端点分别通过结构化JSON和函数调用验证；三路并发
  结构化输出均在11～14秒完成。生产OSS配置已在仓库外备份后切换为两路助手端点，
  原`agent/terra`配置保留用于回滚。
- 代码将助手端点槽位由1增至2，使响应头超时可切换独立端点；单次Agent输出预算
  从1200收紧至700 token，不改变服务端已核定的动作、价格或手数。
- 原90.1秒红灯命令修复后14.4秒返回四个完整字段。全量2442项测试、Vite构建、
  FC打包和`git diff --check`通过，FC与Vercel均已发布。生产泰坦股份连续两次
  更新分别39.5秒和34.1秒保存，五段说明完整，均无Agent超时或联合决策不可用。

### 100元开源时序基础模型训练：Task 1契约切片（2026-09-17）

- 执行规格为
  `docs/rebuild/open-tsfm-training-plan-under-100-rmb-2026-09-17.md`，状态已切换为
  执行中。100元为总支出硬上限，累计90元停止；预算不得用于降低统计或交易门禁。
- 新增`foundation_return_contract.py`，冻结一个主候选、两个固定教师、三个强基线、
  五折顺序、种子17/29/43、七分位网格、训练窗口上限、100元预算分配和默认
  `UNAVAILABLE`发布策略。
- 契约通过规范化JSON生成稳定`experimentId`和`configHash`；`experiment.json`
  采用原子写入，重复执行只接受完全一致配置，配置漂移或文件篡改均拒绝恢复。
- 官方Hugging Face revision已冻结：TTM `90-30-ft-l1-r2.1`为
  `cd2ad2a54ba5531fbcf6ba3b7a763a6e14223680`，TimesFM 2.5为
  `1d952420fba87f3c6dee4f240de0f1a0fbc790e3`，Chronos-2为
  `29ec3766d36d6f73f0696f85560a422f50e8498c`。
- 新增5项契约测试，覆盖稳定哈希、预算超限、模型角色、可变revision、重复种子、
  非扩展折、幂等冻结和篡改拒绝；结果`5 passed`，Ruff及`git diff --check`通过。
- 真实`experiment.json`尚未生成：其数据、特征和标签hash必须等Task 2数据集封存后
  写入，禁止用占位hash冒充已冻结实验。当前支出0元，生产活动指针未修改。

### 100元开源时序基础模型训练：Task 2数据与切分（2026-09-17）

- 新增虚拟基础数据集，绑定 sealed ranking、market 与 execution-label 上游，不复制
  10,223,999×90 日序列；读取时按证券和决策日流式组装90日OHLCV、复权因子及
  可用时间。基础库SHA-256为`6f8a8323…e3a8fa3`。
- 全市场参考标签明确为10万元完整成交、次日复权开盘至第五日复权收盘、扣双边费用
  和卖出滑点的`REFERENCE_FULL_FILL_FEE_ADJUSTED_5D`。分钟执行标签仅覆盖
  19,486个`reference_100k`样本，其中17,161个成交、2,325个未成交；无覆盖保持缺失。
- 新增可恢复的Tushare `daily_basic`市值数据集，封存2,536日、10,223,999行，
  数据库SHA-256为`41dc40b6…1ddc3a`。北交所新旧代码按时点解析；770条上游股本/
  市值口径异常保留原值并写质量标记，市值仅用于抽样分层，不作为模型输入。
- 冻结五折 expanding walk-forward：两段校准各63日、purge/embargo各5日、测试
  各252日。每折按交易日等额，并按板块、市值五分位、流动性五分位和费后收益方向
  分层确定性抽样1,500,000条；概率和逆概率权重均已保存，训练/测试泄漏为0。
- 抽样库SHA-256为`a9ac39af…73b382`，切分清单SHA-256为
  `b5711163…845f13`。最终实验ID为
  `foundation-return-100rmb-v1-9ded11c2a2508c69`，状态`UNAVAILABLE`，
  成本台账为0元，活动生产指针SHA-256仍为`b67344b1…10f17d5`。
- 30项聚焦测试、Ruff和`git diff --check`通过。Task 2完成；尚未训练树模型、
  基础模型或轻量分布头，未读取第5折指标，未修改生产模型或交易事实。

### 100元开源时序基础模型训练：Task 3树模型基线（2026-09-17）

- 新增`probabilistic_baseline_runner.py`，锁定CatBoost 1.2.10、XGBoost 3.4.1、
  LightGBM 4.7.0，统一使用31个点时排序特征、10万元完整成交费后5日收益、方向
  标签、七分位网格和确定性逆概率权重；浮点批量费用与Decimal权威实现逐笔对齐。
- 四个基线族为历史方向发生率/分位、CatBoost MultiQuantile、XGBoost
  `reg:quantileerror`和七个独立LightGBM quantile。统一输出Brier、log loss、
  pinball、WIS、80% coverage、RankIC和Top-10费后收益；有限样本分位 crossing
  仅在输出端稳定排序，不修改真实标签。
- fold 1先完成20日/3轮smoke；正式参数随后冻结为120 trees、depth 6、
  learning rate 0.05、seed 20260917。前四折生成16份receipt并通过开发OOF审计后，
  才写`development-freeze.json`并一次性开放第五确认折；确认折未调参或重排候选。
- 最终每个family封存6,290,297条五折OOF、1,260个测试日，共20份receipt；
  唯一键重复0、折间重叠0，跨family日期、证券、费后标签和样本权重完全一致。
  协议SHA-256为`9933b815…f732e3`，最终OOF审计SHA-256为
  `58814e6e…4325f0`，正式产物占用约930MB。
- 五折聚合WIS为LightGBM 0.029253、CatBoost 0.029264、XGBoost 0.029451、
  历史分布0.030598。LightGBM五折Top-10费后增量为+0.1922%，但第五确认折为
  -0.0332%；其原始80% coverage仅77.14%。因此Task 3只提供冻结基线，不满足
  校准、稳定收益、执行、容量、Agent或前瞻门禁。
- 12项聚焦测试、Ruff和`git diff --check`通过。累计付费仍为0元，最终manifest
  保持`UNAVAILABLE`；未修改生产模型指针、账户、持仓、余额或成交事实。

### 100元开源时序基础模型训练：Task 4基础模型筛选（2026-09-17）

- 复核发现 teachers-v1/v2/v3 将过去已成熟的`r_net_5d`序列误作正式教师输入；
  这些产物降级为预实验，不参与候选选择。teachers-v4 改为 Task 2 的90日点时
  `adjustedClose`，每个样本固定预测未来第5个交易步，再用决策日参考收盘价换算
  close-to-close费后代理，并只在`probabilityCalibration`吸收次日入口gap和模型残差。
- 两个开发折各包含63个校准日和252个测试日，每日确定性选择50只，共31,500行；
  `contexts[-1]`与`referenceClose`逐行零偏差，预测步长全为5，执行日严格晚于决策日。
  teacher-data SHA-256为`d4e90229706bc6ce68a46f66e2f8c00fea496e7d666e24c432905f19ce40cb3d`。
- 官方冻结模型均成功：TTM r2.1在MPS推理24.33秒，TimesFM 2.5在CPU推理
  326.66秒，Chronos-2在CPU推理192.18秒；三者各31,500行，原始点预测与分位
  均有限且为正，付费均为0元。异常路径另支持封存终态失败receipt，不静默丢弃。
- 最终比较只使用fold 1/2，并在相同25,200个`(date, instrument)`测试键上抽取
  CatBoost、XGBoost、LightGBM OOF；三类基线与教师使用同一63日加权经验残差
  校准。exact key重复、目标偏移、receipt/file hash漂移均失败关闭。
- 同校准基线的最佳WIS/Brier来自XGBoost，最佳RankIC来自CatBoost，最佳Top-10
  费后增量来自LightGBM。TTM、TimesFM、Chronos的预注册综合分分别为
  `-0.5506`、`-0.6472`、`-0.8374`；平均Top-10费后增量分别为
  `-0.0942%`、`-0.1565%`、`-0.2255%`。
- TTM虽然相对最好，但fold 1 WIS/Brier分别劣化10.14%/7.03%，且两折费后增量
  均负；TimesFM两折费后增量均负；Chronos两折RankIC均负。三者均触发固定灾难
  门禁，未强行选择主候选。最终`teacher-screening-v6`结果SHA-256为
  `7be756a242be0f1edaf1ae9ddae999cca0c608b4d6aebdb7c127c6e77a1bca45`，
  `selectedModel=null`、`STOPPED_ALL_CANDIDATES_FAILED`。
- 54项Task 1-4聚焦测试、Ruff、产物幂等复跑与`git diff --check`通过。累计付费
  仍为0元；Task 5-8按计划停止，未打开第5折、未修改生产指针或交易事实。

### 本机预测训练数据清理（2026-09-17）

- 用户确认当前预测模型无继续价值后，终止Task 5-8，不再进行轻量微调、五折三种子、
  校准或发布。删除`~/.local/share/stock-platform`下除运行/恢复资产外的全部训练
  数据集、OOF、模型、回测、教师产物和`foundation-teacher-runtime-v1`。
- 同时删除家目录下可重建的Tushare分片、mainboard历史、重标注工作目录、
  `stock-datasets`、stockdb研究目录，以及仅含TTM、TimesFM、Chronos权重的
  Hugging Face缓存。清理前计划删除量约114.9GiB；磁盘可用空间由21GiB增至137GiB，
  实际释放约116GiB。
- 保留`backups`、`migrations`、`joint-releases`、`joint-candidates`和
  `daily-joint-cycles`。主备份
  `platform-20260916T1432Z.dump` SHA-256仍为
  `72fc6c42a4236931a9e9ade76d2ddd9a1467cbe57a3094701d2a128b59c029ba`，
  活动SHADOW指针SHA-256仍为
  `b67344b1aeecc1fb6e0bb81f2e31bf943c882a335fbb81d1a6505c33810f17d5`。
- 清理后生产`https://www.tedixtf.cn/__health`返回`ok`。未删除仓库源码、文档、
  外部配置、账户迁移记录或数据库备份，未修改OSS、FC、生产指针或交易事实。

### 选股候选源、分位门槛与净效用排序（2026-09-17）

- 保留盘中回踩承接、盘中资金先行、收盘趋势回踩、收盘蓄势突破四条量价公式；
  没有增加新的固定公式。新增质量底线和巨潮官方事件两个候选来源标识，所有候选
  仍需经过统一价格合同和生产决策模型，不自动生成成交。
- 全市场行情补充行业字段；公式内固定成交额/换手率要求改为全市场或行业内分位，
  仅保留成交额3000万元、换手率0.3%的绝对可交易底线。分位与候选源身份写入
  机会账本，供未来无泄漏回放。
- 最终排序改为`pFill * expectedNetR - tailRiskPenalty`，其中
  `tailRiskPenalty = 0.35 * max(0, -netRLowerBound)`；CatBoost/联合排序分降为
  次级排序。候选源权重支持按`BULL/CHOP/RETREAT`切换，但仅接受通过完整门禁的
  `selection-candidate-policy.v1`快照。
- Tushare季度财务数据共175,984条。质量源在全市场历史上完成5折、789个测试
  交易日、6,757,772个股票日的费后回放，覆盖率93.77%；聚合费后5日平均收益
  +0.1771%，10日块bootstrap单侧95%下界-0.2941%。五折中第1、3、5折非正，
  未通过收益稳定性门禁。
- 现有480,468条含费机会回放覆盖1,314个交易日，但历史记录没有保留四条公式的
  来源身份；巨潮事件也没有完整历史全股票池归档。财务修订可用时点和历史行业
  归属尚未认证。因此动态权重和官方事件不能执行严格时间外评估。
- 研究快照保持`RESEARCH / productionEligible=false`，未上传活动OSS路径、未
  修改线上模型。完整报告见
  `docs/rebuild/selection-candidate-policy-backtest-2026-09-17.md`；仓库外机器
  报告SHA-256为`f5d04ee1377b80e6d7a058eacf14220cb0d840108be1034f9dda8cd90357cbc9`。

### 候选池 Agent 仲裁接入（2026-09-17）

- 新增`opportunity-agent-selection.v1`合同。Agent最多接收8只已通过生产量化评分、
  合法价格路径和账户容量检查的候选，最多选择3只；输出中的股票代码、价格路径和
  证据ID均由服务端逐项校验，Agent不能新增股票、修改价格、手数或风险边界。
- 每只候选必须同时引用量化模型、市场资金、板块资金、个股资金和事件复核证据。
  巨潮公告保留`VERIFIED`状态；联网新闻只保存为
  `SEARCH_RESULT_UNVERIFIED`线索。同日但缺少精确发布时间的新闻不进入可用证据，
  防止盘中未来信息泄漏。
- 新增按账户哈希隔离的Agent结果、运行状态和完整证据轨迹。快照绑定候选来源时间、
  模型版本、已有价格路径和账户容量指纹；任一输入变化即过期。普通雷达GET只读取
  快照，不触发付费搜索或模型调用。
- 用户手动刷新时先并行更新板块、公式和官方事件，再运行候选池Agent。只有同批次
  Agent明确选择、量化为`READY/DIRECT`、价格合同合法且账户仍有容量的候选可保持
  `READY`；Agent未运行、失败、无证据或未选择时统一降为`AVOID`，不回退纯规则买入。
- 机会卡片展示Agent优先级、买入理由、暂停条件、失效条件及量化/三级资金/公告新闻
  证据状态；加入自选和成交仍由用户手工操作。选股来源快照新增Agent运行ID、模型、
  排名和证据引用，便于后续成交归因。
- 相关合同、证据组装、账户隔离、失败关闭、API聚合、刷新顺序和UI测试已通过；
  后续独立复审补齐五类边界：搜索失败不得冒充无新闻、未核验新闻不能单独授权、
  来源指纹与最终证据批次分离、刷新先作废旧选择并等待异步来源完成、跨实例运行
  使用相邻时间桶不可覆盖租约，最终组合和摘要按门禁后状态重算。最终独立复审
  未发现P0/P1。
- 全量Node测试`2467/2467`通过，Vite生产构建、FC打包、Ruff和
  `git diff --check`通过。只读浏览器预览在1440×1000和390×844均无横向溢出、
  控制台错误或失败请求，移动端操作按钮高度44px。
- Python后端全量为`322 passed, 7 failed, 7 errors`。失败来自本轮未修改的旧后端：
  交易规则核验截止日仍为2026-09-16，而当前测试日为2026-09-17；另有
  `test_decision_service`清理顺序导致测试库`notifications -> outbox`外键残留，
  后续固定证券ID重复。Ruff通过；本次未越界修改这些用户并行开发文件。
- 本次尚未部署生产，也未触发自动下单、成交写入或真实账号浏览器操作。

### 本地隔离整站走查与交易录入加固（2026-09-17）

- 原计划使用ScoutQA执行探索测试，但其本地页面接入依赖`cloudflared`，被企业终端
  安全策略明确禁止。未绕过策略；已终止全部远端执行并清除本机ScoutQA OAuth凭据。
- 改用本机Chrome/Playwright和仓库内综合测试账户，新增
  `scripts/verify-local-system-walkthrough.mjs`。脚本只允许`127.0.0.1`，阻断外网，
  不读取浏览器凭据，不提交成交，不写OSS。
- 覆盖登录/注册、今日作战/盘面研究、持仓与自选/资金与仓位、交易复盘/预警、
  研究助手、候选纳入取消、执行计划超限输入，以及候选池Agent盘中/次日切换；
  覆盖390、768、1440三档视口、控制台、网络失败、横向溢出和可访问名称。
- 发现并修复两项真实问题：恢复代码把密码规则退回任意字符，现由UI、前端提交和
  Node API共同强制5位ASCII数字；待执行计划输入超过剩余手数或小数手数时，原按钮
  仍可点击，现UI直接禁用且账本层继续失败关闭。
- 登录门户补充语义化`h1`；移动端股票名称、Agent证据链接及依据展开控件达到触摸
  尺寸。修复后本地整站报告为7个页面/视口组合、0个critical/high/medium/low问题，
  `cloudWrites=0`、`submittedTrades=0`。
- 生产域仅做无凭据只读冒烟：390和1440视口均HTTP 200、无横向溢出、无控制台或
  网络错误。生产当前仍是旧密码输入合同，需随下一次正式发布更新；本次未部署。
- 聚焦32项测试、全量Node测试`2470/2470`、Vite生产构建、FC打包和
  `git diff --check`通过。旧`verify-v3-workbench.mjs`同步到10分钟复核口径后
  四视口通过；旧`verify-system-upgrade.mjs`仍含已被新版工作台替代的深层DOM断言，
  当前整站验收以新的本地隔离脚本为准。

每个切片记录实际修改、验证命令、结果、限制和提交，不记录密钥、完整私人账户或无关日志。只有实际通过的任务才更新完成状态。

### 选股模块重构（已完成并部署，2026-09-17）

用户决策：①一级模块"今日作战"→"选股"；②全市场扫描召回→模型排序→Agent 结合盘面/资金/公告精选并给出买入策略与时机；③选股与盘面研究并入同一页两个子视图；④旧候选池 Agent 方案删除、按新方案重建；⑤新 qlib/LightGBM 排序模型直接上线替换旧生成模型；⑥Agent 不选时展示 Top 候选供参考。设计见 `docs/rebuild/stock-picker-redesign-2026-09-17.md`。

前置探明（据两份后台调研 + 本地探测）：
- 选型：召回层复用全池扫描器 `fetchTailPickRealtimePool`（`api/_tail_pick_data.js`，翻页并发 + `unique.length===total` 强校验）+ Alpha158 因子公式（MIT）+ 量价/资金规则；排序层用 LightGBM（backend 已装 4.7.0）+ 现成 `ranking_*`/`execution_walk_forward` 骨架，**不新装 qlib**（默认 py3.14 装不上，backend 有 py3.12.13）。Agent 精选复用 `_stock_fund` 三级资金 + 豆包 `_ai_search`/开源 `_searxng_search`。排除 backtrader、时序模型直接上线、TimesFM3.0 商用权重、RD-Agent 直出因子。
- 全A股全池分页扫描唯一实现在 `fetchTailPickRealtimePool`，被公式/尾盘/预催化共用。排序模型骨架：`ranking_model_trainer.py`(LGBMRanker/HGB, schema `ranking-model-bundle.v1`)、`ranking_walk_forward.py`、`release_service.py`。

S1（已完成）：`shared/appShell.js` today 段 label/shortLabel 改"选股"、描述改为全市场扫描召回；`src/App.jsx` 子视图改"选股结果/盘面研究"，选股结果挂载 `StockPickTab`、盘面研究挂载 `ResearchTab`。内部路由 key `today` 保留（深链/快捷键/历史栈不受影响）。`app-shell`、`mobile-navigation` 测试通过；无测试断言旧文案。收尾：`src/components/PlanTab.jsx` 空态文案"从今日作战加入机会"→"从选股加入候选"，为唯一残留的生产可见旧文案。`src/components/TodayTab.jsx`（及其 `OpportunityRadar` 子树）已无任何生产消费者（App.jsx 不再挂载、无 src import），但仍被 9 个 UI 契约测试作为回归基线读取，非零消费者；按纵向切片"先验收替代、再以零消费证据删除、禁止盲删"的纪律，其物理删除留待后续独立切片，本轮不动测试基线。

S2（已完成，commit 36c63fe）：`/api/stock_pick` 召回服务。`shared/stockPick.js` 合同（规则召回分/候选归一化/模型优先排序/不可用降级）；`api/_stock_pick_recall.js` 复用 `fetchTailPickRealtimePool` 全池扫描→规则预筛(取Top72)→`scoreCandidatesWithDecisionModel` 取排序分(缺失回落规则分并标注)→归一化快照；`api/_stock_pick_store.js` OSS+内存回退+单飞锁；`api/stock_pick.js` GET读快照/POST run。测试 `stock-pick-contract`(5) + `stock-pick-recall`(4) 通过。

S4（已完成，commit 66485f5）：Agent 精选。`shared/stockPickAgent.js` 合同逐项复校——只能选候选池内 code、买入价须落当日高低价带(否则回落现价)、仓位≤10%、不生成成交、无有效选择降级 NO_SELECTION。`api/_stock_pick_agent.js` runner 组装盘面(market_snapshot)+三级资金(_stock_fund)+公告新闻(_ai_search,过滤未来时间)→调 assistant LLM(700token/90s/json)精选，输出买入策略(entryPrice/positionPctMax/plan)+时机(trigger/window/nextSession)+反方+失效。`stock_pick.js` action:'agent' 路由，Agent不选/不可用时返回 Top 候选参考(标注未经精选)。测试 `stock-pick-agent`(5)+`stock-pick-agent-runner`(5) 通过。

S5（已完成，commit 98ac4c4）：前端选股结果视图。`src/stockPickClient.js`；`src/components/StockPickTab.jsx`(全市场召回+Agent精选按钮/精选卡/Top候选参考态/候选池表)；`src/App.jsx` 选股结果子视图改由 StockPickTab 渲染，移除旧 AdaptiveWorkbench(TodayTab) 死导入；`precision.css` 选股样式+移动端单列。vite build 通过；93 项相关测试通过(含 ui-quality-contract 无回归)。

待续：S3 离线 LightGBM 排序模型(用 backend py3.12+现成 ranking_* 骨架, 费后 walk-forward, 产真实模型分)；S6 零消费者删旧候选池 Agent 后端(opportunity_agent_selection*/OpportunityRadar*/TodayTab, 注意 opportunity_radar 账本族是训练标签来源)；S7 全量验证+浏览器走查+验收记录。

S7（已完成）：全量 Node `2479 passed, 0 failed`（S1 PlanTab 文案收尾后复跑仍全绿，选股相关切片无回归）；`vite build` 通过（`StockPickTab`、`ResearchTab` 独立 chunk 均产出）；`npm run package:fc` FC 打包通过（`.fc-package` 就绪）。`/api/stock_pick` handler 契约实测：GET 空态返回 `{snapshot:null,...}`；POST 未鉴权 401；鉴权后端异常已加固为 401(不再 500)。本地隔离浏览器走查(390/768/1440)：Agent 精选卡渲染、买入策略/时机文案齐全、候选池渲染、0 横向溢出、0 控制台错误(一次性预览已清理)。至此 S1–S7 七个纵向切片全部交付验收，选股模块（全市场召回→LightGBM 排序→Agent 精选）已并入生产。

S1 遗留死代码清理（已完成）：先建全量引用图证明零生产消费者后删除。删除组件簇 `TodayTab.jsx`、`OpportunityRadar.jsx`、`OpportunityRadarContent.jsx`、`OpportunityIntradayNav.jsx`、`OpportunityCandidateRow.jsx`、`AdaptiveWorkbench.jsx`、`AdaptiveWorkbenchPreview.jsx` 与 `src/opportunityRadarClient.js`（其唯一 src 消费者即前述组件）；删除簇独占 dev 预览 `test/ui/opportunity-radar-preview.*`、`test/ui/strategy-runtime-preview.*` 与簇独占测试 `opportunity-radar-ui`、`candidate-pool-mobile-layout`；移除 `planStore.js` 孤儿包装 `todayCommandList`（`buildTodayCommandList` 保留——position_workbench 与 today-command 测试仍消费）。9 个混合测试（polling-policy、opportunity-training-status、formula-selection-ui、tail-pick-ui、ui-quality-contract、stock-tags-ui-contract、position-workbench、market-overview-ui、sector-forecast-ui）做外科式删除：仅摘除已失效的 TodayTab/OpportunityRadar 集成断言，保留全部仍有效的组件契约。保留（活跃消费者仍在）：后端 `opportunity_radar` 账本族（训练标签来源、position_workbench 消费）、`AccountRiskStrip`(PlanTab)、`SectorForecastSettings`(SectorForecast)、`StrategyPatternEvidence`(DecisionSummary)。级联发现：`FormulaSelection`/`SectorForecast`/`TailPick` 因原仅挂载于已删的 OpportunityRadar，现无 src 挂载点，作为真实功能代码予以保留、未连带删除（属另一次产品决策，不在本次死代码清理范围）。验证：Node `2451 passed, 0 failed`（=2479−28 簇内测试）；`vite build` 通过；`npm run package:fc` 通过。

前端选股旧功能删除（已完成，用户决策 A：这三个功能不再需要）：删除公式选股/板块前瞻/尾盘拾金的**前端展示层**——组件 `FormulaSelection`、`FormulaSelectionCandidate`、`FormulaSelectionProgress`、`FormulaPrice`、`TailPick`、`TailPickCandidate`、`TailPickResults`、`SectorForecast`、`SectorForecastProgress`、`SectorForecastSettings`、`SectorConceptExplanation`，及前端客户端 `formulaSelectionClient.js`、`formulaPriceView.js`、`sectorForecastClient.js`、`sectorForecastView.js`、`tailPickClient.js`（均确认为簇独占、零簇外消费者）。随删纯簇 UI/客户端测试 `formula-selection-ui`、`tail-pick-ui`、`sector-forecast-ui`、`sector-forecast-view`、`formula-price-view`、`tail-pick-client`。外科式编辑 3 个混合测试：`sector-concept-explanation`（保留 shared/agent 逻辑断言，删去读已删组件与簇 CSS 的两条 UI 断言）、`user-facing-language`、`user-facing-ai-language`（各移除对已删组件文件的一条断言）。**严格保留后端**（活跃消费者仍在）：`_tail_pick_data.js`(fetchTailPickRealtimePool——选股召回 `_stock_pick_recall`、`_formula_selection_data`、pre_catalyst 的全市场事实源)、`formula_selection.js`/`sector_forecast.js`/`tail_pick.js` API 及其被 `_advice_timer`、`_daily_report_v3`、`ai.js` 等保留链路的消费；`shared/sectorConceptExplanation.js`(概念释义 agent 能力，agent/account/planStore 消费)。相应后端逻辑测试（*-api/*-formula/*-policy/*-price/strategy-deep-scoring 等）全部保留未动。验证：Node `2403 passed, 0 failed`（=2451−48）；`vite build` 通过（无断链）；`npm run package:fc` 通过。

S3 历史阻塞（已解除）：训练依赖 sealed 全A股历史行情 market-dataset；有效 Tushare 兼容源配置到仓库外后，已完成数据集构建、训练、回测和发布。

S3 数据构建（已完成）：有效端点为 Tushare 兼容镜像；token 仅存仓库外配置。数据集根 `~/stock-datasets/market.sqlite3`，dataset-id `ashare-2022-2024`，窗口 20220101-20241231。reference、names、725 个 daily 分区及 seal 全部完成。构建中修复真实数据缺陷 commit 31094da：宇宙外且无 lifecycle 的退市脏事实应丢弃；builder 测试 49/49。

S3（已完成训练、接入与上线，commit e413aed）：全链路真跑完成。market-dataset.v4 封存(372万日线/373万复权因子/725交易日, SHA256 cd1e1cf…)→ ranking-dataset.v1(330万横截面样本, SHA256 7f8763f…)→ train-ranking-model LightGBM LambdaRank(lightgbm-lambdarank-v1, 200迭代)。**时间外费后回测真实指标**(train≤20240108/calib→20240611/confirm→20241224, 5日embargo, 67万确认样本)：日均 Rank IC 0.0709、Top10 毛收益 5.04% vs 市场 1.20%(超额+3.84pp)、Top10 费后(10bps压力) 4.94%、胜率 57.8%。`ranking-score-snapshot.v1` 含 5246 只股票，已上传生产 OSS `stockpick/ranking-score-snapshot.json`；原始字节 SHA-256 `faffd766e742446f189f68b8b07c3d6db463e84132386f42f549829b74da37c9`，上传后回读校验一致。`api/_stock_pick_recall.js` 优先使用真实模型分，缺失时明确回落规则分。

S6（需谨慎解耦，非盲删）：旧候选池 Agent 仍有活跃共享消费者——`api/opportunity_radar.js` 调用 agent selection；`api/position_workbench.js`(持仓工作台,保留) 用 `readOpportunityRadarSnapshot`；`api/formula_selection.js` 用 `_opportunity_radar_ledger_store`；`AdaptiveWorkbench` 经 DEV 预览(`?preview=adaptive`)仍引用 OpportunityRadar 链。生产 App.jsx 选股结果视图已改由 StockPickTab 渲染，不再挂载 TodayTab/AdaptiveWorkbench。删除须先从 opportunity_radar 解耦 agent selection 调用，再删纯 Agent 文件，保留 radar 聚合与 ledger(position_workbench 消费)。未在本轮盲删，避免破坏共享生产代码。

S6（已完成，先解耦后零消费者删除）：`opportunity_radar.js` 摘除候选池 Agent selection 全部逻辑（3个agent import、readAgentSelection参数、agentResult分支、applyOpportunityAgentSelection应用块、死助手 agentSourceState/currentAgentSnapshot/applyAccountBudgetGate/summarizeRows、未用 sha import），回归纯聚合(sector/formula/tail/preCatalyst+ledger+训练状态)，position_workbench 调用不传 agentScope 故行为不变。删除纯 Agent 文件(均为 untracked 恢复文件)：`api/opportunity_agent_selection.js`、`_opportunity_agent_selection.js`、`_opportunity_agent_selection_store.js`、`_opportunity_agent_evidence.js`、`shared/opportunityAgentSelection.js` 及 `test/opportunity-agent-selection.test.js`。确认删除前这些文件仅互相 import、无外部消费者。`src/opportunityRadarClient.js` 的 runOpportunityAgentSelection/invalidate 改为 AGENT_RETIRED 占位(不再调已删端点)，移除死 mutate。`App.jsx` 移除 DEV 预览 AdaptiveWorkbenchPreview 挂载。`account-auth.test.js` 改用 stock_pick handler 覆盖鉴权；`opportunity-radar-api.test.js` 删除过时 agent 选择用例；`opportunity-radar-ui.test.js` 断言改 AGENT_RETIRED。保留：opportunity_radar 聚合、ledger/baseline/settlement/outcome 族(position_workbench/formula_selection 消费, 且为训练标签来源)、OpportunityRadar*/TodayTab/AdaptiveWorkbench 组件(radar UI 仍可用, 只是不再跑候选池 Agent)。全量 2479 passed + vite build + FC 打包通过。commit 见 git log。

生产发布（2026-09-17）：同一源码版本已发布到 Vercel 与阿里云 FC。Vercel生产别名 `https://stock-dashboard-one-plum.vercel.app`，FC `https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run`，主域 `https://www.tedixtf.cn`。主域与 FC 的 `/api/stock_pick`、`/api/quote?code=600519` 均返回 HTTP 200；两套前端均加载 `StockPickTab`，Vercel 构建的 API base 指向当前 FC。Vercel仅托管静态前端，自身 `/api/*` 404 属设计行为。

### 选股三模式与 Agent 工具闭环（已完成并部署，2026-09-17）

设计与判断见 `docs/rebuild/stock-picker-three-mode-agent-design-2026-09-17.md`。
当前采用一个已通过时间外费后验证的 LightGBM 基础排序模型 + 盘中机会/提前布局/
次日关注三种场景策略，不发布三个未经独立训练、五折三种子验证和校准的模型。
Agent 采用一个选股 runtime 与一个独立端点的三种严格模式；结果、单飞锁和运行轨迹
按账号及模式隔离。原内部 `sector` key 保留兼容旧 OSS 配置，模型配置页和后端角色
标签已从“板块前瞻”改为“选股”，选股 Agent 不再使用通用 assistant 端点。

完成能力：
- `shared/stockPickModes.js` 固化三模式、人工次日名单、逐股票首报价幂等和运行轨迹
  合同；`_stock_pick_store.js` 按账号哈希和模式保存 Agent 结果/轨迹/锁，不在路径中
  写明文账号。
- `_stock_pick_tools.js` 提供市场盘面、候选实时行情、个股资金、公告新闻四类受限工具；
  `_stock_pick_agent.js` 支持最多4轮“工具调用→结果感知→继续分析”，Agent 漏调模式
  必要证据时由服务端补齐，再进行最终结构化判断。
- `stockPickAgent.v2` 服务端复校候选 code、价带、仓位和 T+1；盘中 `BUY_NOW` 缺少
  当日实时价时降级为 `WAIT_TRIGGER`，提前布局仓位上限固定收紧到5%，次日初始判断
  固定为观察而非成交。
- 次日关注支持人工勾选并保存（最多12只）、手动复算和次日首笔实时价自动复算。
  自动复算只覆盖人工名单，并按账号+股票+交易日幂等；一只股票先出报价不会阻断
  其他名单项。
- 前端重构为全市场候选基线、三模式分段控件、模式主动作、次日专属操作带、Agent
  执行轨迹、当前模式结果和模型候选池。轨迹展示阶段、进度、工具名称、返回摘要、
  校验、异常和最终状态，不展示隐藏思维链。
- 匿名 GET 只可读取市场候选快照，不返回任何账号 Agent 结果或次日人工名单。

验收：
- 选股/端点专项 `50 passed, 0 failed`；
- 全量 Node `2421 passed, 0 failed`；此前其它切片记录的选股 UI 文案断言已改为读取
  `shared/stockPickModes.js` 的真实模式定义，不再保留已知失败；
- `vite build` 通过，`StockPickTab` 独立 chunk 正常；
- `npm run package:fc` 通过；
- 本机 Chrome 139 隔离预览，390/768/1440 三视口实际点击盘中机会→提前布局→次日
  关注，勾选第二只股票并保存：三视口横向溢出均为0、轨迹5个事件、结果和候选池
  正常、控制台 error/warning 为0。截图位于
  `artifacts/stock-pick-{390,768,1440}.png`。

发布状态：已从同一源码部署 Vercel 与 FC；未修改生产模型指针、未写交易事实。

### 每日量化学习闭环（已部署并完成首轮生产门禁，2026-09-17）

- 新增 `learning-event.v1` 与 `learning/v1/` OSS 命名空间。事件路径由类型、交易日、
  账户哈希和稳定事件 ID 派生，写入使用 `x-oss-forbid-overwrite`；相同内容可幂等
  重放，不同内容覆盖同一 ID 会返回 `LEARNING_EVENT_IMMUTABLE_CONFLICT`。
- 选股全市场召回、Agent 精选和持仓联合决策均旁路写入预测事件。学习写入失败只记录
  告警，不阻断原建议和人工交易流程；训练载荷不含账号明文、理由全文、新闻原文或
  密钥。
- `cron_learning` 每个交易日 17:20 由 FC Timer 运行：结算预测后 5 个交易日的
  选股收益/MFE/MAE/方向命中；遍历 OSS 账户只读快照，导出人工执行和
  `validationComplete && learningEligible` 的真实持仓归因。计划、未成交建议、
  模拟成交和未成熟结果不进入实际结果样本。
- 每次结算发布不可变 `learning-training-view.v1` 和 `learning-manifest.v1`。
  GitHub Actions 北京时间 01:15 调用受 `CRON_KEY` 保护的端点做幂等复核，只从
  `learning/v1/views/` 下载脱敏视图。
- `qlib-service/learning_pipeline.py` 训练 LightGBM LambdaRank 选股 challenger 和
  LightGBM Huber 持仓净 R challenger。两者使用时间外末 3 个交易日验证和
  17/41/97 三种子；选股要求平均及最差种子 Top5 收益均优于现役排序分，持仓要求
  平均及最差种子 MAE 均优于原计划净 R。
- 持仓训练通过 `decisionPlan.decisionId` 连接预测与真实归因。动作、参考价、止损/
  止盈和模型概率是训练特征；成交后的净收益、MFE/MAE、持有时长仅作为标签或评估
  指标，禁止进入特征矩阵，避免标签泄漏。
- 样本门禁：选股至少 60 个成熟样本/12 个交易日；持仓至少 30 个真实成熟样本/
  10 个交易日。不足时返回 `SKIPPED_INSUFFICIENT_MATURED_DATA`；未优于基线返回
  `CHALLENGER_REJECTED`。训练产物只写
  `learning/v1/training-runs/`，报告固定
  `productionPointerChanged=false`，不存在生产模型指针更新步骤。
- 验收：学习合同/采集/结算/训练视图与调度专项、Python 训练门禁均通过；全量
  Node `2429/2429`、Vite 生产构建和 FC 打包通过。
- GitHub Actions 复用默认分支已有的 `OSS_BUCKET`、`OSS_ACCESS_KEY_ID` 和
  `OSS_ACCESS_KEY_SECRET`，不再依赖新增 secrets；训练下载器将读取路径硬限制为
  `learning/v1/manifests/` 与 `learning/v1/views/`，上传路径硬限制为
  `learning/v1/training-runs/`。后续仍建议将现有 OSS 凭据轮换为等价前缀最小权限
  RAM 身份。工作流已进入默认分支并完成真实自动运行验证。

恢复与首轮生产验收（2026-09-17 21:46）：
- 确认此前没有训练进程卡死；新 `daily-learning-challengers` 工作流仅存在于本地
  `rebuild/platform-v1`，未进入 GitHub 默认分支，因此远端定时任务从未注册。
- 已从同一源码重新构建并部署 FC 与 Vercel。FC
  `learning-settlement-timer` 已启用（工作日 17:20），
  `/api/cron_learning` 未授权请求返回 401，`/api/stock_pick` 返回 200；
  主域与 Vercel 生产别名均返回 200。
- 已人工触发首轮生产结算：选股预测 0 条成熟样本；持仓 6 条成熟样本、覆盖 2 个
  交易日。随后完成训练视图下载、三种子门禁执行和不可变训练运行上传，结果为
  `NO_CHALLENGER_PROMOTION`，两路均为
  `SKIPPED_INSUFFICIENT_MATURED_DATA`，`productionPointerChanged=false`。
- 当前不是故障状态，而是正常采样等待状态：选股门槛为 60 条/12 个交易日，持仓
  门槛为 30 条/10 个交易日。FC 会继续每日结算并积累脱敏样本。

GitHub Actions 自动运行验收（2026-09-17 22:21）：
- PR #1 将完整学习闭环以 squash commit `934082e` 合并到 `main`；PR #2 将仅限
  学习流水线文件的 `push` 验证入口以 commit `b1e8805` 合并到 `main`。既有北京
  时间 01:15 的定时触发保持不变。
- `daily-learning-challengers` run
  [#35232945353](https://github.com/anthony67158/Short-term-stock-trading/actions/runs/35232945353)
  成功，`train` job `105241486528` 用时 46 秒。不可变合同测试、Python 门禁测试、
  最新脱敏视图下载、challenger 训练、不可变运行上传和诊断产物上传全部成功。
- 本次读取 manifest
  `learning/v1/manifests/2026-09-17/54afefa1c6b0307f95a8802b5a0fee7cc85bfc7eb87cb27d75f1ca6b67b302b5.json`；
  manifest、view 路径与规范化内容哈希一致。训练视图为选股 0 行、持仓 0 行，未将
  缺少 `decisionId` 预测关联的历史真实结果强行配对。
- 运行报告上传至
  `learning/v1/training-runs/2026-09-17/b50569a0ff072031bd3e/report.json`：
  总状态 `NO_CHALLENGER_PROMOTION`，两路均为
  `SKIPPED_INSUFFICIENT_MATURED_DATA`，`productionPointerChanged=false`。
  线上 `stockpick/ranking-score-snapshot.json` 的最后修改时间早于本次运行，
  确认 Actions 未切换生产模型。

生产保障加固（2026-09-17）：
- FC `learning-settlement-timer` 从单次 17:20 调整为工作日 17:20 与 17:35 两次
  幂等运行，后一次作为结算和训练视图发布重试。GitHub Actions 的脱敏视图下载、
  challenger 训练和不可变上传各自最多尝试 3 次；上传遇到 OSS 409 时回读字节，
  内容相同视为幂等成功，不同内容按不可变冲突失败。
- Actions 获得最小 `issues: write` 权限。重试耗尽后按北京时间日期创建
  `[daily-learning] YYYY-MM-DD failed` Issue；同日再次失败只追加运行链接，不暴露
  账号、训练行或密钥。GitHub 原生失败状态仍保留。
- 训练下载器按北京时间计算应存在的最近工作日结算日期。最新 manifest 早于该日期、
  结算时间早于当日 17:00、manifest/view 日期或时间戳不一致、路径越出
  `learning/v1/views/`、规范化内容哈希不匹配时均失败关闭，不再静默复用盘中、
  陈旧或被修改的训练视图。
- 选股 T+5 标签升级为 `stock-pick-t5-fee-v2`：复用
  `A_SHARE_STANDARD_V1`，以 100 股标准手数计算买入与卖出双边佣金、印花税和过户费；
  `returnPct`、MFE、MAE、方向命中和 +2% 路径命中均使用费后现金流。训练视图仅接收
  `feeAdjusted=true` 的 v2 结果，旧毛收益事件保留审计但不参与训练，并可由新事件 ID
  重新结算为 v2。
- 验证：全量 Node `2449/2449`、Python 学习管道 `4/4`、Vite 生产构建、FC 打包和
  YAML 解析均通过；生产 OSS 最新 manifest/view 已完成只读新鲜度与哈希冒烟验证。
- PR #4 已以 commit `2d61838` 合并到 `main`。合并触发的
  `daily-learning-challengers`
  [run #35237807902](https://github.com/anthony67158/Short-term-stock-trading/actions/runs/35237807902)
  成功，`train` job `105258129102` 用时 52 秒；最新不可变报告为
  `learning/v1/training-runs/2026-09-17/b8eedb063aa34b8a04af/report.json`，
  两路均因 0 条合法关联样本安全跳过，且 `productionPointerChanged=false`。
- FC 已重新部署，远端触发器回读确认
  `CRON_TZ=Asia/Shanghai 0 20,35 17 * * 1-5`。部署后受保护结算实测成功：
  只读扫描 4 个账户，导出 64 条人工执行、识别 6 条成熟实际归因；因这些历史归因
  没有可验证的预测 `decisionId`，最新训练视图仍为选股 0 行、持仓 0 行，没有强行
  配对或伪造训练样本。
