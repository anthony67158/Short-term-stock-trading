# 部署与配置总览（改代码前必读）

> 本文件是"改一次代码 → 同步更新阿里云后端 + Vercel 前端"的操作手册。
> 记录了全部**地址、别名、部署命令、环境变量清单**。密钥值不写在此文件里（在 `.env` / FC 控制台 / Vercel env 中），这里只列 **key 名 + 用途 + 存放位置**。
>
> 最后更新：2026-08-03

---

## 0. 一句话架构

```
浏览器
  │
  ├── 静态前端(dist/)  ──────────────►  Vercel（只托管前端，不跑任何 API）
  │        │ 构建时 VITE_API_BASE 烘入 FC 地址，浏览器直连后端
  │        ▼
  └── /api/*  ───────────────────────►  阿里云 FC「主服务」 stock-dashboard
                                              （server.js 一个进程托管 dist + api/*.js 12个接口，含 SSE 流式）
                                                │
                                                ├── 数据读写 ──► 阿里云 OSS（替代 Vercel Blob）
                                                ├── 大模型   ──► LLM_BASE_URL（原样，未迁百炼）
                                                └── 量化打分 ──► 阿里云 FC「量化微服务」 quant-score（FastAPI 容器镜像）
```

- **前端**：Vercel 静态托管。构建时把 `VITE_API_BASE` 注入为 FC 主服务地址，浏览器**直连** FC（不走 Vercel 反代，因为 Vercel 外部 rewrite 对 SSE 流式不稳定）。
- **后端主服务**：阿里云 FC 3.0 自定义运行时（`custom.debian10`，`node server.js`）。同一进程既托管 `dist/` 静态前端，又处理 `/api/*` 的 12 个 handler。
- **量化微服务**：独立的第二个 FC 函数（容器镜像 / FastAPI），主服务通过 `QUANT_URL` 调它。
- **存储**：阿里云 OSS，key 前缀 `accounts/` `sectorflow/` `dailyreport/`（与原 Vercel Blob 前缀一致，迁移后直接可读）。
- **大模型**：保持原 `LLM_BASE_URL` / `LLM_API_KEY`，未迁移。

---

## 1. 线上地址与账号（现网实际值）

| 项目 | 值 |
|---|---|
| **Vercel 前端稳定别名**（发给用户/自用，链接不变） | https://stock-dashboard.vercel.app |
| Vercel 团队别名 | https://stock-dashboard-natmatthias172-6311s-projects.vercel.app |
| Vercel scope（团队 slug） | `natmatthias172-6311s-projects` |
| Vercel 项目名 | `stock-dashboard` |
| **FC 主服务地址**（VITE_API_BASE 就是它） | https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run |
| **FC 量化微服务地址**（QUANT_URL） | https://quant-score-nlxgclpdbu.cn-hangzhou.fcapp.run |
| 阿里云地域 | `cn-hangzhou`（华东1-杭州） |
| 阿里云 AccountID | `1730034925594178` |
| Serverless Devs access 别名 | `default`（配置在 `~/.s/access.yaml`） |
| **GitHub 仓库** | https://github.com/anthony67158/Short-term-stock-trading （分支 `main`） |

> FC 地址里的随机串（`znrlekbzit`、`nlxgclpdbu`）是函数创建时生成的，**同一函数重复部署地址不变**。若删除重建函数会变——那时需同步更新 Vercel 的 `VITE_API_BASE` 和 `.env` 的 `QUANT_URL`。

---

## 2. 目录结构（关键文件）

```
stock-dashboard/
├── src/                       前端 React 源码（Vite）
│   ├── apiBase.js             ★ API 基址逻辑：读 VITE_API_BASE 拼 /api 前缀
│   ├── App.jsx / components/  UI
│   ├── ai.js review.js planStore.js authStore.js hooks.js
├── api/                       ★ 后端 12 个 handler（只跑在 FC，Vercel 不部署）
│   ├── _*.js                  下划线开头=内部库(不注册为路由): _llm _blob _lib _ai_prompts _ta _kb ...
│   └── ai.js agent.js account.js daily_report.js market.js board.js quote.js
│       stock_detail.js sectors.js sector_history.js stocks.js search.js
├── server.js                  ★ FC 自定义运行时入口(托管 dist + /api/*，SSE 兼容)
├── dev-server.js              本地 API 调试服(node dev-server.js，端口 3000)
├── s.yaml                     ★ FC 主服务 Serverless Devs 部署描述
├── qlib-service/              量化微服务(独立 FC 函数)
│   ├── app.py                 FastAPI 打分+走势预测
│   ├── Dockerfile / Dockerfile.aliyun
│   └── s.yaml                 ★ 量化微服务部署描述(容器镜像)
├── scripts/migrate-blob-to-oss.mjs   数据迁移(Vercel Blob → OSS，一次性)
├── vercel.json                Vercel 静态构建配置(buildCommand/outputDirectory/缓存头)
├── vite.config.js            本地 /api 代理到 localhost:3000
├── .vercelignore             ★ 排除 api/ server.js s.yaml 等，Vercel 只上前端
├── .fcignore                 ★ 排除 src/ docs/ 等，FC 只上运行所需
├── .env                      ★ 全部密钥(本地部署 source 它；不进 git)
├── .env.oss.example          环境变量模板(可提交)
└── docs/
    ├── 部署与配置总览.md       ← 本文件
    └── 阿里云FC-OSS迁移手册.md  迁移背景与一次性步骤
```

---

## 3. 环境变量清单（key 名 + 存放位置）

值不在本文件。三处需要保持一致：**本地 `.env`** / **FC(s.yaml 用 `${env(...)}` 从 shell 读)** / **Vercel(仅前端构建变量)**。

### 3.1 FC 主服务需要（部署前 `set -a; source .env; set +a`）
| Key | 用途 |
|---|---|
| `LLM_BASE_URL` `LLM_API_KEY` | 大模型网关（原样，未迁移） |
| `LLM_MODEL` `ADVISOR_MODEL` `AGENT_MODEL` `DAILY_MODEL` | 各场景模型名（s.yaml 已 pin 默认值） |
| `FINNHUB_KEY` | 行情数据 |
| `QUANT_URL` `QUANT_KEY` | 调量化微服务的地址与鉴权 |
| `OSS_REGION` `OSS_BUCKET` `OSS_ACCESS_KEY_ID` `OSS_ACCESS_KEY_SECRET` | OSS 存储（必填） |
| `OSS_ENDPOINT` `OSS_PUBLIC_BASE` | OSS 可选（同区内网 endpoint / CDN 读取基址） |

### 3.2 FC 量化微服务需要
| Key | 用途 |
|---|---|
| `QUANT_IMAGE` | ACR 容器镜像地址 |
| `QUANT_KEY` | 与主服务同一个值（鉴权） |

### 3.3 Vercel 需要（前端构建变量，Production）
| Key | 值 |
|---|---|
| `VITE_API_BASE` | FC 主服务地址 `https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run` |

> Vercel 上历史遗留的后端变量（`FINNHUB_KEY` `QUANT_URL` `LLM_*` `BLOB_READ_WRITE_TOKEN` 等）现在**没用了**（后端已不在 Vercel），留着无害，可择机清理。
> `BLOB_READ_WRITE_TOKEN` 仅一次性迁移脚本用，FC 运行时不需要。

---

## 4. 改代码后的标准操作流程 ★

判断改的是哪一层，对应部署。多数功能改动会同时涉及前后端。

### A. 只改了前端（src/ 下，UI/交互/前端逻辑）
```bash
cd stock-dashboard
export VC_TOKEN="<Vercel Token>"       # 见第 6 节，勿明文打印
export PATH="$HOME/.npm-global/bin:$PATH"
npx vercel deploy --prod --yes --scope natmatthias172-6311s-projects --token="$VC_TOKEN"
# 校验
curl -s -o /dev/null -w "%{http_code}\n" https://stock-dashboard.vercel.app/   # 期望 200
```
> Vercel 会在云端 `npm run build`，自动读取已配置的 `VITE_API_BASE`，把 FC 地址烘进 bundle。

### B. 只改了后端主服务（api/ 或 server.js）
```bash
cd stock-dashboard
npm run build                          # server.js 也托管 dist/，一起打进 FC
set -a; source .env; set +a            # 注入所有环境变量给 s.yaml 的 ${env(...)}
export PATH="$HOME/.npm-global/bin:$PATH"
s deploy                               # 部署 FC 主服务(access=default)
# 校验
curl -i https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run/__health          # 期望 200 ok
curl -s "https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run/api/sectors?type=industry&sort=main" | head -c 200
```

### C. 前后端都改了（最常见）
先做 B（部署 FC 后端），再做 A（部署 Vercel 前端）。两者独立，顺序不强制，但后端先上可避免前端调到旧接口。

### D. 改了量化微服务（qlib-service/）
```bash
cd stock-dashboard/qlib-service
# 若改了 app.py 需重新构建并推镜像到 ACR，再：
set -a; source ../.env; set +a
s deploy
curl -i https://quant-score-nlxgclpdbu.cn-hangzhou.fcapp.run/            # 健康检查
```

### E. 每次改完都要做
```bash
cd stock-dashboard
git add -A
git commit -m "描述本次改动"
git push origin main
```

---

## 5. 部署要点/踩坑（务必遵守）

1. **Vercel 只上前端**：`.vercelignore` 已排除 `api/ server.js s.yaml .env` 等。绝不能让 `api/*.js` 被 Vercel 当 Serverless Function（会触发 Hobby 版 12 函数上限，也会抢在直连之前拦 `/api/*`）。
2. **前端直连 FC，不走 Vercel 反代**：因为 SSE 流式（AI 逐字输出）在 Vercel 外部 rewrite 下不稳定。靠 `VITE_API_BASE` 直连 + 后端 CORS `Access-Control-Allow-Origin *`。
3. **FC 只上运行所需**：`.fcignore` 排除 `src/ docs/ scripts/ *.tar.gz` 等；但会上传 `dist/ api/ server.js node_modules package.json`。所以**部署 FC 前必须先 `npm run build`**。
4. **改 FC 地址要联动**：只要 FC 函数被删除重建导致地址变，必须同步：① Vercel `VITE_API_BASE`；② `.env` 的 `QUANT_URL`（若变的是量化服务）；③ 本文件第 1 节。
5. **Deployment Protection(SSO)**：Vercel 项目此前已关闭（公开可访问返回 200）。只需关一次，后续部署不会重新开启。
6. **密钥安全**：Token/AK/SK 只存环境变量与 `.env`（`.env` 已在 `.gitignore`），绝不写进代码、日志、提交、或本文件。

---

## 6. 凭证存放位置（值不在此，仅指路）

| 凭证 | 存放位置 |
|---|---|
| Vercel Access Token | 用时 `export VC_TOKEN=...`；来源 https://vercel.com/account/tokens |
| 阿里云 AK/SK（Serverless Devs） | `~/.s/access.yaml`（别名 `default`） |
| 全部运行时密钥（LLM/OSS/FINNHUB/QUANT） | `stock-dashboard/.env`（不进 git） |
| Vercel 前端构建变量 | Vercel 项目 → Settings → Environment Variables（`VITE_API_BASE`） |

---

## 7. 常用校验命令

```bash
# 前端
curl -s -o /dev/null -w "%{http_code}\n" https://stock-dashboard.vercel.app/     # 200
# 后端健康
curl -i https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run/__health         # 200 ok
# 后端 API
curl -s "https://stock-dashboard-znrlekbzit.cn-hangzhou.fcapp.run/api/sectors?type=industry&sort=main" | head -c 200
# 确认前端 bundle 已烘入 FC 地址
idx=$(curl -s https://stock-dashboard.vercel.app/ | grep -o '/assets/index-[A-Za-z0-9]*\.js' | head -1)
curl -s "https://stock-dashboard.vercel.app$idx" | grep -c "fcapp.run"            # ≥1
```
