# 每日量化持续训练（冠军-挑战者，只升不降）
# 触发：每天 UTC 01:30（北京时间 09:30 前后，收盘后新成熟标签已可用）+ 手动。
# 机制：拉最新日线→重建数据集→挑战者 vs 冠军(leak-free holdout AUC 对拍)→过护栏才晋级→上传 OSS。
#       量化服务 model_lib 每 1h 从 OSS 热更新，故上传即上线，无需重部署 FC。
name: daily-retrain

on:
  schedule:
    - cron: "30 1 * * 1-5"   # 周一至周五 UTC 01:30
  workflow_dispatch: {}       # 支持手动触发

concurrency:
  group: daily-retrain
  cancel-in-progress: false

jobs:
  retrain:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: qlib-service   # 训练代码所在子目录(按仓库实际路径调整)
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        # 固定大版本,避免 numpy/lightgbm/sklearn 某天发新版导致的隐性构建失败(与线上 requirements 对齐)
        run: pip install "numpy>=1.26,<2.1" "scipy>=1.11,<1.15" "lightgbm>=4.3,<4.8" "scikit-learn>=1.3,<1.6" "oss2>=2.18,<3.0"

      - name: Preflight - 行情数据源连通性自检
        # 海外 CI 出口 IP 访问新浪/腾讯 CN 行情接口常被限流/不可达 —— 这是本任务历史性每日失败的根因。
        # 这里先探测,连不通就直接跳过后续训练并正常退出(不判红报警),日志里也能一眼看清是数据源问题。
        id: preflight
        run: |
          python3 - <<'PY'
          import urllib.request, sys
          H={'User-Agent':'Mozilla/5.0','Referer':'https://finance.sina.com.cn/'}
          def ok(u):
              try:
                  return len(urllib.request.urlopen(urllib.request.Request(u,headers=H),timeout=20).read())>50
              except Exception as e:
                  print('  probe fail:', u.split('?')[0], repr(e)); return False
          sina=ok('https://vip.stock.finance.sina.com.cn/quotes_service/api/json_v2.php/Market_Center.getHQNodeData?page=1&num=3&sort=amount&asc=0&node=hs_a')
          tx=ok('https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sh600000,day,,,60,qfq')
          print(f'[preflight] sina={sina} tencent={tx}')
          # 冷 runner 上 pool_cache.json 不在仓库(已 gitignore),股票池必须现拉新浪、个股日线必须现拉腾讯,
          # 故两者都通才继续;任一不通即标记 skip(跳过当日重训,不判红)。
          open('preflight.txt','w').write('ok' if (sina and tx) else 'skip')
          PY
          echo "reachable=$(cat preflight.txt)" >> "$GITHUB_OUTPUT"

      - name: Run champion-challenger retrain
        if: steps.preflight.outputs.reachable == 'ok'
        env:
          PYTHONUNBUFFERED: "1"
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          OSS_BUCKET: ${{ secrets.OSS_BUCKET }}
          OSS_ENDPOINT: ${{ secrets.OSS_ENDPOINT }}
          QUANT_MODEL_PREFIX: quantmodel/
        run: |
          python3 retrain_daily.py --pool 600 --bars 700 --horizon 5

      - name: Skip notice (数据源不可达)
        if: steps.preflight.outputs.reachable != 'ok'
        run: echo "::warning::行情数据源(新浪/腾讯 CN)在本次 CI 出口 IP 下不可达,已跳过当日重训(线上保持冠军不变,非代码故障)。"

      - name: Show latest decision
        if: always()
        run: tail -n 1 retrain_history.jsonl || echo "(no history)"

      - name: Upload audit log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: retrain-history-${{ github.run_id }}
          path: qlib-service/retrain_history.jsonl
          if-no-files-found: ignore
